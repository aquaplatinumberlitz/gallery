Fix 3 cleanup issues in gallery-repo at /home/ubuntu/gallery-repo.

Read each file before editing. After ALL fixes, run `./test.sh fast` to verify nothing breaks, then commit with message "cleanup: address phase 10 pre-release cleanup items" and push.

---

## Fix 1: Misleading comment in backend/scan.py (line 1)

Current:
```python
"""Authorize media file paths for serving under the registered-library security model.
```

The media endpoints (image/video/thumbnail/preview) don't check library registration — they only check PATH_SAFETY_ROOT. The comment is misleading. Change to:

```python
"""Authorize media file paths for serving under the PATH_SAFETY_ROOT containment model.
```

And also update line 24:
```python
    The path must pass the ``PATH_SAFETY_ROOT`` containment check via
```

---

## Fix 2: Remove dead scan query keys from frontend/src/query/keys.ts

Two functions have ZERO consumers in the codebase. Remove them:

- Line 37: `scan: (path: string, limit: number) => ["scan", normalizeQueryPath(path), limit] as const,`
- Line 46: `scanInfinite: (path: string, limit: number) => ["scan-infinite", normalizeQueryPath(path), limit] as const,`

That's it — just delete those 2 lines. Verify with `grep -r "queryKeys\.scan\b"` or `grep -r "queryKeys\.scanInfinite\b"` that no consumers exist.

---

## Fix 3: Remove ScanResponse, inline into BrowseResponse

Read frontend/src/types/index.ts fully first. The relevant section:

a) `ScanResponse` interface (around line 104) — has `index_source` with `"warm_db" | "direct_scan" | "mixed" | "catalog"` and is NOT imported by any file directly.

b) `BrowseResponse` (around line 116) — `extends ScanResponse` and overrides `index_source: "catalog"`.

Fix: Inline the fields from ScanResponse directly into BrowseResponse. Delete the ScanResponse interface entirely. The result should be:

```typescript
export interface BrowseResponse {
  folders: FileNode[];
  media: MediaNode[];
  next_media_cursor: string | null;
  total_images: number;
  total_videos: number;
  total_assets: number;
  index_source: "catalog";
}
```

Check that BrowseResponse is the only consumer — `LibraryScanResponse` is a separate interface (around line 321) and should NOT be changed.

---

## Verify

After all 3 fixes, run:
```
cd /home/ubuntu/gallery-repo && ./test.sh fast
```

It must pass: backend ruff check/format, backend tests, frontend vitest, frontend typecheck (vue-tsc), and production build.

Then:
```
git add -A && git commit -m "cleanup: address phase 10 pre-release cleanup items" && git push
```
