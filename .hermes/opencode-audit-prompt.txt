Audit the metadata_store refactor commits in range 3332eef..8d7e664.

Context: backend/metadata_store.py (5,542 lines, 165 definitions) was split into backend/metadata_store/ subpackage with 16 modules. The refactor used `git mv` to preserve history, incremental extraction, and __init__.py re-export shim for backward compatibility.

For each commit, review:
1. Any import cycles or missing re-exports in __init__.py
2. Any function that uses another module's symbols via `from .module import` inside function body (late import) — flag if it should be top-level
3. Any public API change that breaks consumers
4. Any dead re-exports (symbols re-exported but no longer imported by any consumer)
5. Any module-level mutable state that could cause issues (singletons, global state)

Run:
cd /home/ubuntu/gallery-repo
git diff 3332eef^..8d7e664 --stat

Then for each issue found, provide the file, line, and suggested fix.

Key checks:
- `backend/metadata_store/__init__.py` — every re-export should have a consumer. Grep the codebase for `from backend.metadata_store import X` or `backend.metadata_store.X`.
- Cross-module imports within metadata_store/ should be clean (no cycles).
- Tests still patch `_DB_INITIALIZED` correctly via `ms._db._DB_INITIALIZED`.
- The `_upsert_asset_conn` signature changed (added `reactivate_existing: bool = True` param) — check all callers are compatible.

Summary format at end:
- Total issues found
- Severity breakdown (P0=wrong behaviour, P1=structural concern, P2=cosmetic)

CHỈ ĐỌC VÀ BÁO CÁO, KO SỬA GÌ.
