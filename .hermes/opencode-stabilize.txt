Final stabilization tasks. Repo: /home/ubuntu/gallery-repo, branch: main.

## Task 1: Add regression test for DB path source-of-truth

**File mới:** `backend/tests/test_db_path_consistency.py`

Test rằng `_connect()` và `initialize_database()` dùng cùng DB path khi config thay đổi:

```python
"""Test that _connect and initialize_database use the same DB path."""

from pathlib import Path


def test_db_path_source_of_truth_is_consistent(monkeypatch, tmp_path):
    """Patch config and verify _connect and initialize_database use same path."""
    import backend.config as cfg
    from backend.metadata_store._db import _connect, _gallery_metadata_db_path
    from backend.metadata_store._schema import initialize_database

    test_db = tmp_path / "test_consistency.db"
    monkeypatch.setattr(cfg, "GALLERY_METADATA_DB", test_db)

    assert _gallery_metadata_db_path() == test_db, (
        "_gallery_metadata_db_path should return the patched path"
    )

    conn = _connect()
    initialize_database()
    conn.close()

    assert test_db.exists(), f"Database should be created at {test_db}"


def test_db_path_works_with_relative_import(monkeypatch, tmp_path):
    """Verify that patching config makes _connect and _schema use the right path."""
    import backend.config as cfg
    from backend.metadata_store._db import _connect

    test_db = tmp_path / "test_relative_consistency.db"
    monkeypatch.setattr(cfg, "GALLERY_METADATA_DB", test_db)

    conn = _connect()
    cur = conn.execute("PRAGMA database_list")
    rows = cur.fetchall()
    conn.close()

    assert any(test_db.name in str(row[2]) for row in rows), (
        f"Connection should point to {test_db}, got: {rows}"
    )
```

**Verify:**
```bash
cd /home/ubuntu/gallery-repo/backend
python -m pytest -q tests/test_db_path_consistency.py --tb=long
```

## Task 2: Add docs staleness check script

**File mới:** `scripts/check_docs_staleness.py`

```python
"""Check docs for stale patterns after refactoring."""

import sys
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parents[1]

# Patterns that should NOT appear in non-archived docs
BAD_PATTERNS: dict[str, str] = {
    "metadata_store.py": (
        "metadata_store is now a package (backend/metadata_store/), "
        "not a single file"
    ),
    "Backend modules live flat in `backend/`": (
        "backend now has domain packages (metadata_store/)"
    ),
    "Backend modules live flat in backend/": (
        "backend now has domain packages (metadata_store/)"
    ),
}

DOC_FILES = [
    p
    for p in REPO_ROOT.rglob("*.md")
    if "archived" not in p.parts
    and "node_modules" not in p.parts
    and ".venv" not in p.parts
    and ".hermes" not in p.parts
]


def main() -> int:
    failed = False
    for doc in sorted(DOC_FILES):
        try:
            text = doc.read_text(encoding="utf-8")
        except Exception:
            continue
        for pattern, reason in BAD_PATTERNS.items():
            if pattern in text:
                rel = doc.relative_to(REPO_ROOT)
                print(f"STALE [{rel}]: found {pattern!r} — {reason}")
                failed = True

    if failed:
        print("\nFix the stale patterns above before marking docs as reviewed.")
        return 1
    print("Docs staleness check passed.")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

**Verify:**
```bash
cd /home/ubuntu/gallery-repo
python scripts/check_docs_staleness.py
```

## Task 3: Tag checkpoint

```bash
cd /home/ubuntu/gallery-repo
git tag -a stable-metadata-store-split-2026-06-23 -m "Stable after metadata_store split cleanup, catalog rename, and docs sync"
git push origin stable-metadata-store-split-2026-06-23
```

## Quy trình

1. Tạo `backend/tests/test_db_path_consistency.py`
2. Tạo `scripts/check_docs_staleness.py`
3. Chạy verify
4. Commit + push
5. Tag + push tag

## Verify tổng

```bash
cd /home/ubuntu/gallery-repo/backend
python -m pytest -q tests/ --tb=short 2>&1 | tail -3
ruff check backend/ scripts/
python scripts/check_docs_staleness.py
```

## Commit message
```
chore: add DB path consistency test, docs staleness check, and stable tag

- Add test_db_path_consistency.py verifying _connect and initialize_database
  use the same DB path when config is patched
- Add scripts/check_docs_staleness.py to prevent stale pattern regression
- Tag stable-metadata-store-split-2026-06-23
```
