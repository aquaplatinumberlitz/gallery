You are updating the documentation files for an AI Art Gallery project. Your task is to read the current codebase and update these 4 documentation files:

1. README.md (repo root)
2. docs/ARCHITECTURE.md
3. docs/THIRD_PARTY_LIBRARIES.md
4. docs/testing/README.md

Start by reading the current version of each file, then explore the codebase to find what has changed.

Key changes since last review (2026-06-15):
- SortDropdown.vue partially replaced by SortSelect.vue (shadcn-vue Select component for toolbar sort controls)
- LibraryInspector.vue now uses @tanstack/vue-virtual for virtualized table rows (~15 DOM rows instead of 100)
- New metadata performance diagnostic test: frontend/tests/metadata-performance.spec.ts
- Focus ring fixes across all shadcn shared components (focus -> focus-visible)
- shadcn-vue Select components used in metadata toolbar (All models, All prompts, sort)
- LibraryInspector.vue uses SortSelect.vue instead of SortDropdown.vue
- MobileHeader.vue still uses SortDropdown.vue (not migrated)
- toolbarTrigger.ts shared class exists but SortSelect.vue uses its own shadcn SelectTrigger styling

For each file, the review date should be updated to 2026-06-16.

For README.md:
- Keep the project description accurate
- Update feature list if anything new has been added
- Keep quickstart, config, and development instructions accurate

For docs/ARCHITECTURE.md:
- Keep the backend module table accurate (check for new/deleted files)
- Keep the frontend component/route descriptions accurate
- Mention virtualized Library Inspector table
- Mention shadcn-vue Select for toolbar controls

For docs/THIRD_PARTY_LIBRARIES.md:
- Add @tanstack/vue-virtual if not present (used in LibraryInspector.vue)
- Verify shadcn-vue (reka-ui) select components are listed
- Check if any libraries can be removed or updated
- Keep the table accurate with current integration contracts

For docs/testing/README.md:
- Add metadata-performance.spec.ts to the test catalog
- Update any outdated test descriptions
- Keep the common commands table accurate

Important rules:
- Do not add made-up features or libraries
- Do not remove real features or libraries
- Read actual file contents to verify claims
- The frontend code is in frontend/src/
- The backend code is in backend/
- Package.json and requirements.txt are authoritative for dependencies
- Preserve the existing document structure and tone
