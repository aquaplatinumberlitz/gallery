Mày review audit sau đây của GPT cho metadata_store refactor range 3332eef..8d7e664. Với mỗi issue, cho biết ĐỒNG TÌNH / PHẢN ĐỐI / KHÔNG CHẮC, kèm lý do và dẫn chứng code.

--- GPT AUDIT ---

## Verdict
Không thấy blocker rõ ràng. Refactor sát plan, split đúng hướng, backward-compatible. Nhưng có 2 điểm cần xử lý:

**Issue 1 — Behavior change trong `_upsert_asset_conn`**
Trước split, `_upsert_asset_conn()` luôn set `offline = 0, deleted_at = NULL`.
Sau split, thêm parameter `reactivate_existing: bool = True` và SQL thành:
```sql
offline = CASE WHEN ? THEN 0 ELSE assets.offline END
deleted_at = CASE WHEN ? THEN NULL ELSE assets.deleted_at END
```
Trong `metadata_persist.py`, nhiều caller gọi `_upsert_asset_conn(..., reactivate_existing=False)`.
Đây là behavior change thật, không pure refactor. Metadata persist không nên resurrect offline/deleted asset.
Risk: Medium. Cần test: upsert metadata không reactivate offline asset.

**Issue 2 — `GALLERY_METADATA_DB` shim có thể lệch source-of-truth**
- `_db._connect()` đọc trực tiếp `config.GALLERY_METADATA_DB`
- `_schema` có helper `_gallery_metadata_db()` đọc qua shim: `from . import GALLERY_METADATA_DB`
Nếu test/plugin patch `metadata_store.GALLERY_METADATA_DB` mà ko patch `backend.config.GALLERY_METADATA_DB`, thì `_connect()` mở DB khác với `initialize_database()`.
Risk: Medium (nếu có consumer cũ patch shim) / Low (nếu toàn bộ code luôn patch config).

**Issue 3 — `__init__.py` vẫn là mega re-export (569 dòng)**
Eager import tất cả module con. Private API bị "official hóa" tạm thời.
Không blocker, nhưng nên cleanup sau phase compatibility.
Risk: Low-Medium.

**Positive findings:**
- 159/159 functions, 6/6 classes preserved (AST check)
- Không có static cycle
- 85-90% sát plan

--- CONTEXT ---
Repo: gallery-repo
Branch: main
All 708 tests pass. Current __init__.py is 569-line shim.
Các file tham khảo:
- backend/metadata_store/_asset_store.py
- backend/metadata_store/metadata_persist.py
- backend/metadata_store/_db.py
- backend/metadata_store/_schema.py
- backend/config.py

--- YÊU CẦU ---
Đọc code thực tế để verify từng claim:
1. Issue 1: `_upsert_asset_conn` — check signature, SQL, và callers trong metadata_persist. Behavior change thật ko? Có intentional ko?
2. Issue 2: `GALLERY_METADATA_DB` — check _db._connect() dùng config trực tiếp hay qua shim. Check _schema._gallery_metadata_db(). Xác nhận divergence có thật ko.
3. Issue 3: dead re-exports count thực tế. Phân tích risk.

CHỈ ĐỌC VÀ BÁO CÁO, KO SỬA GÌ.