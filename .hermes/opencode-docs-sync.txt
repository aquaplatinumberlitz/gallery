Docs sync phase 3 — audit và sửa từng file .md thủ công, ko dùng script.
Repo: /home/ubuntu/gallery-repo, branch: main.

## Những thay đổi codebase gần đây cần phản ánh trong docs

1. **metadata_store split**: `backend/metadata_store.py` (file đơn) → `backend/metadata_store/` package (17 modules)
2. **catalog/ rename**: `backend/catalog/service.py` → `backend/scan_worker.py`, `backend/catalog/status_builder.py` → `backend/metadata_store/status_store.py`
3. **Fixtures move**: `tests/fixtures/catalog_status/` → `backend/tests/fixtures/catalog_status/`
4. **Dead re-exports cleanup**: `__init__.py` trimmed 569→282 dòng

## Files cần audit và sửa (theo thứ tự ưu tiên)

### 1. README.md
- Line 127: `├── metadata_store.py` → `├── metadata_store/` (package)
- Thêm dòng: `│   ├── status_store.py` (nếu có section liệt kê modules)
- "Last reviewed: 2026-06-21" → "Last reviewed: 2026-06-23"
- Kiểm tra API Summary section: nếu còn endpoint ko tồn tại (vd /api/scan), bỏ đi

### 2. docs/ARCHITECTURE.md
- **Line 25**: `Backend modules live flat in backend/.` → `Backend modules are mostly flat, with selected domain packages.`
- **Line 44**: `metadata_store.py` → xoá dòng này, thay bằng section riêng phía dưới
- **Thêm section sau bảng (sau line 55):**

```
| `scan_worker.py`           | Background catalog scan/rebuild worker, durable job queue, queue_scan/queue_rebuild/run_once/start/stop |
```

- **Thêm section "Domain Packages":**

```
### Domain Packages

| Package / Module         | Purpose                                                              |
| ------------------------ | -------------------------------------------------------------------- |
| `metadata_store/`        | SQLite data access layer: schema, search, CRUD, job queue            |
| `metadata_store/_db.py`  | SQLite connection, init flags, shared constants                      |
| `metadata_store/_schema.py` | Schema creation, v8-to-v9 migration                               |
| `metadata_store/types.py` | Shared dataclasses and exceptions                                   |
| `metadata_store/path_utils.py` | Path normalization and overlap helpers                          |
| `metadata_store/library_store.py` | Library CRUD                                                   |
| `metadata_store/job_store.py` | Catalog job queue management                                       |
| `metadata_store/rebuild_store.py` | Staging area for rebuild operations                            |
| `metadata_store/browse_store.py` | Browse listing queries                                          |
| `metadata_store/file_index.py` | File index operations                                             |
| `metadata_store/folder_index.py` | Folder index operations                                           |
| `metadata_store/metadata_queue.py` | Metadata index job queue                                        |
| `metadata_store/metadata_persist.py` | Metadata persistence helpers                                  |
| `metadata_store/search_store.py` | FTS5 search queries                                              |
| `metadata_store/inspector_store.py` | Library Inspector data access                                  |
| `metadata_store/_asset_store.py` | Shared asset upsert helper                                       |
| `metadata_store/_resources.py` | Image resource parsing helpers                                   |
| `metadata_store/status_store.py` | Unified catalog status query builder                            |
| `tests/`                  | Backend test suite (pytest)                                         |
```

- Line 48: `catalog rebuild helpers` → `scan worker rebuild helpers` (trong mô tả indexer.py)
- Line 49: `refresh.py` — OK
- Line 50: `watcher.py` — OK
- "Last reviewed: 2026-06-22" → "Last reviewed: 2026-06-23"

### 3. docs/METADATA_PARSING.md
- Line 6: `backend/metadata_store.py` → `backend/metadata_store/` (package)
- Cập nhật link nếu có

### 4. docs/THIRD_PARTY_LIBRARIES.md
- Line 19: `backend/metadata_store.py` → `backend/metadata_store/` (package)

### 5. docs/testing/TEST_CATALOG.md
- Các references tới "catalog" test files (test_catalog_*) vẫn đúng (tên file ko đổi)
- Chỉ sửa nếu có path/stale reference
- "Status: Maintained" → kiểm tra và update nếu cần

### 6. docs/testing/README.md
- Tương tự TEST_CATALOG.md

### 7. docs/plans/METADATA_STORE_SPLIT_PLAN.md
- Move vào `docs/archived/`
- Thêm header: "Status: Archived — plan is complete, current architecture is in docs/ARCHITECTURE.md"

### KHÔNG sửa
- docs/archived/ (giữ nguyên)
- docs/research/ (research docs tham khảo lịch sử, ko cần cập nhật)
- docs/reports/
- backend/, frontend/, scripts/ (ko thuộc phase này)

## Quy trình

Với mỗi file, đọc toàn bộ nội dung, kiểm tra kỹ từng dòng, sửa stale/outdated/mâu thuẫn.

## Verify
```bash
cd /home/ubuntu/gallery-repo
git diff --stat
# Kiểm tra ko còn references tới metadata_store.py (file cũ) trong README và docs chính
grep -n "metadata_store\.py" README.md docs/ARCHITECTURE.md docs/METADATA_PARSING.md docs/THIRD_PARTY_LIBRARIES.md 2>/dev/null
```

## Commit + push
Commit message: `docs: sync architecture docs with current codebase after metadata_store split and catalog rename`
