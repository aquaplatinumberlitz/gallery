Fix 5 issues từ audit metadata_store split (range 3332eef..8d7e664).

Repo: /home/ubuntu/gallery-repo
Branch: main
Hiện tại: 708 tests pass, ruff clean.

## Issue 1: Dead re-exports (Step 12 cleanup)

**File:** `backend/metadata_store/__init__.py` (569 dòng)
**Vấn đề:** Re-export ~170 symbols nhưng chỉ 52 được import bởi tests.
**Yêu cầu:** Trim chỉ giữ lại các symbol thực sự được import từ bên ngoài. Danh sách 52 symbols cần giữ:

CatalogJobConflict, LibraryOverlapError, MetadataIndexJob, MetadataQueueResult,
_DB_LOCK, _cleanup_ignored_index_conn, _cleanup_stale_index_conn, _connect,
_current_metadata_is_complete, _metadata_job_from_path, _metadata_param,
_replace_image_resources_conn, _scan_folder_counts,
cleanup_ignored_index, cleanup_stale_index,
create_job, create_library,
get_asset_folder_listing, get_cached_dimensions_for_files, get_first_library_root,
get_folder_index_state, get_folder_indexed_paths,
get_job, get_library, get_library_for_path, get_library_progress, get_library_stats,
get_metadata_index_status, get_warm_folder_listing,
index_directory_tree, index_file, initialize_database,
list_active_jobs, list_libraries,
mark_folder_index_incomplete, mark_metadata_jobs_done, mark_metadata_jobs_failed,
mark_metadata_jobs_running, mark_metadata_jobs_stale,
queue_metadata_index_paths, recover_stale_jobs, register_library,
search_index, search_index_fielded, search_metadata,
update_folder_index_state, update_job_state, update_library, update_library_state,
upsert_extracted_metadata, upsert_image_dimensions, upsert_metadata_result

**Cách làm:**
- Đọc file __init__.py hiện tại
- Xoá tất cả import lines cho symbol ko có trong danh sách trên
- Giữ nguyên: logger (dòng cuối), external imports (albums, config, files, metadata_extract, models), `from . import _db as _db`, `from . import path_utils as path_utils`
- KHÔNG xoá dòng logger = logging.getLogger(__name__) ở cuối file

**Verify:** `cd /home/ubuntu/gallery-repo/backend && python -c "from metadata_store import *" && echo OK` + `python -m pytest -q tests/ --tb=short 2>&1 | tail -3`

## Issue 2: Mutable state copies

**File:** `backend/metadata_store/__init__.py`
**Vấn đề:** `_DB_INITIALIZED`, `_DB_INITIALIZED_PATH`, `_DB_LOCK` được re-export từ `_db.py` nhưng assignment vào shim ko propagate về `_db.py`.

**Yêu cầu:** Xoá 3 dòng re-export này khỏi __init__.py:
```python
from ._db import _DB_INITIALIZED as _DB_INITIALIZED,
from ._db import _DB_INITIALIZED_PATH as _DB_INITIALIZED_PATH,
from ._db import _DB_LOCK as _DB_LOCK,
```
Giữ lại các re-export khác từ _db.py (ACTIVE_ASSET_WHERE, _connect, init_db, init_db, etc.)

**Verify:** `cd /home/ubuntu/gallery-repo/backend && python -m pytest -q tests/ --tb=short 2>&1 | tail -3`

## Issue 3: GALLERY_METADATA_DB source-of-truth consolidation

**Vấn đề:** `_db._connect()` đọc `config.GALLERY_METADATA_DB` trực tiếp, trong khi `_schema._gallery_metadata_db()` đọc qua shim (`from . import GALLERY_METADATA_DB`). Nếu ai patch 1 trong 2, DB path bị lệch.

**File 1:** `backend/metadata_store/_db.py`
- Thêm helper function:
```python
def _gallery_metadata_db_path() -> Path:
    """Single source of truth for the metadata database path."""
    from ..config import GALLERY_METADATA_DB
    return GALLERY_METADATA_DB
```
- Sửa `_connect()` dùng `_gallery_metadata_db_path()` thay vì `config.GALLERY_METADATA_DB` trực tiếp

**File 2:** `backend/metadata_store/_schema.py`
- Thay `from . import GALLERY_METADATA_DB` bằng `from ._db import _gallery_metadata_db_path`
- Thay `_gallery_metadata_db()` để dùng `_gallery_metadata_db_path()` từ `_db` thay vì `from . import GALLERY_METADATA_DB`

**Verify:** `cd /home/ubuntu/gallery-repo/backend && python -c "from metadata_store import _db, _schema; print(_db._gallery_metadata_db_path())" && python -m pytest -q tests/ --tb=short 2>&1 | tail -3`

## Issue 4: Test reactivate_existing=False behavior

**File mới:** `backend/tests/test_asset_reactivate.py`
**Test:** metadata upsert ko resurrect offline/deleted asset.

Viết test:
1. Register library
2. Tạo asset qua file_index.index_file
3. Set asset offline + deleted_at trong DB (simulate reconcile)
4. Gọi upsert_extracted_metadata hoặc upsert_image_dimensions cho asset đó
5. Assert asset vẫn offline và deleted_at != NULL

```python
"""Test that metadata operations do not resurrect offline/deleted assets."""

import pytest
from backend.tests.conftest import (
    library_fixture,
    metadata_db,
    temp_assets_root,
)


def test_metadata_upsert_does_not_reactivate_offline_asset(
    metadata_db, temp_assets_root, library_fixture
):
    """upsert_extracted_metadata should not resurrect an offline/deleted asset."""
    import time
    from pathlib import Path
    from backend.metadata_store import index_file, upsert_extracted_metadata
    from backend.metadata_store._db import _connect

    lib = library_fixture
    # Create a test file
    asset_path = temp_assets_root / "test_offline.png"
    asset_path.write_text("fake png")
    
    # Index it
    index_file(str(asset_path))
    
    # Mark as offline + deleted (simulate reconcile)
    conn = _connect()
    conn.execute(
        "UPDATE assets SET offline = 1, deleted_at = ? WHERE path = ?",
        (time.time(), str(asset_path)),
    )
    conn.commit()
    
    # Now upsert metadata — this should NOT resurrect
    from backend.metadata_extract import ExtractedMetadata
    meta = ExtractedMetadata(path=str(asset_path), name="test_offline.png")
    upsert_extracted_metadata(meta)
    
    # Verify still offline
    row = conn.execute(
        "SELECT offline, deleted_at FROM assets WHERE path = ?",
        (str(asset_path),),
    ).fetchone()
    assert row is not None
    assert row["offline"] == 1, "Asset should remain offline after metadata upsert"
    assert row["deleted_at"] is not None, "Asset should remain deleted after metadata upsert"


def test_metadata_upsert_reactivates_active_asset(
    metadata_db, temp_assets_root, library_fixture
):
    """Normal index operation should still reactivate (default reactivate_existing=True)."""
    from pathlib import Path
    from backend.metadata_store import index_file, upsert_extracted_metadata
    from backend.metadata_store._db import _connect

    lib = library_fixture
    asset_path = temp_assets_root / "test_active.png"
    asset_path.write_text("fake png")
    
    index_file(str(asset_path))
    
    # Verify it's active
    conn = _connect()
    row = conn.execute(
        "SELECT offline, deleted_at FROM assets WHERE path = ?",
        (str(asset_path),),
    ).fetchone()
    assert row["offline"] == 0
    assert row["deleted_at"] is None
```

**Verify:** `cd /home/ubuntu/gallery-repo/backend && python -m pytest -q tests/test_asset_reactivate.py --tb=long 2>&1`

## Issue 5: Squash scratch commits

**Yêu cầu:** Squash 3 commit xóa scratch (`5b00831`, `3f48b5c`, `abaacab`) vào parent commits.

Cấu trúc squash:
- `5b00831` (chore: remove scratch files from step 1) → squash vào `3332eef` (step 1)
- `3f48b5c` (chore: remove scratch file from step 2) → squash vào `6499a7f` (step 2)
- `abaacab` (chore: remove scratch file from step 3-5) → squash vào `142f8be` (steps 3-5)

**Cách làm:** Sử dụng `git rebase -i --autosquash` hoặc `git reset --soft` chain.

THẬN TRỌNG: Đã có commit sau các commit này (từ f36cf27 đến 8d7e664). Squash sẽ改写 history. Hỏi user trước khi thực hiện squash. Để comment "SQUASH PENDING — confirm with user first" nếu prompt tự động chạy.

## QUY TRÌNH

1. Fix Issue 1 + 2 cùng lúc (cùng file __init__.py)
2. Fix Issue 3
3. Fix Issue 4 (test mới)
4. Issue 5: hỏi user trước khi squash (rewrite history)
5. Mỗi issue commit riêng
6. Chạy `cd backend && python -m pytest -q tests/ --tb=short` sau mỗi commit
7. Chạy `ruff check backend/metadata_store/`
8. Commit message format: `fix: <mô tả>`
9. Push: `git push origin main`

CHỈ ĐƯỢC SỬA:
- backend/metadata_store/__init__.py
- backend/metadata_store/_db.py
- backend/metadata_store/_schema.py
- backend/tests/test_asset_reactivate.py (file mới)

KHÔNG ĐƯỢC SỬA bất kỳ file nào khác.