Frontend code cleanup phase 2. Repo: /home/ubuntu/gallery-repo, branch: main.

## Task 1: Check for stale API route references

Scan frontend code for API routes that no longer exist on backend:

```
grep -rn "/api/scan\|/api/index/status\|scanDirectory\|rebuildIndex" frontend/src/ --include="*.ts" --include="*.vue"
grep -rn "IndexStatus\|LibraryProgress" frontend/src/ --include="*.ts" --include="*.vue"
```

Nếu còn references tới API routes hoặc types đã xoá, update imports hoặc xoá code chết.

## Task 2: Check for stale imports

Scan frontend/src/ for imports of files that no longer exist:

```
cd frontend
grep -r "from.*@/" src --include="*.ts" --include="*.vue" | grep -v node_modules | grep -v ".test.ts" > /tmp/imports.txt
# Kiểm tra các import path có tồn tại ko
```

## Task 3: Check query keys stale

Kiểm tra `frontend/src/query/keys.ts` — so sánh các query keys với composables còn dùng chúng.

## Task 4: Check frontend tests

```
cd frontend
pnpm run lint
pnpm run lint:tests
pnpm run build
pnpm run test:unit
pnpm run format:check
```

## Task 5: Fix issues found

Với mỗi issue tìm được, fix bằng commit nhỏ.

## KHÔNG sửa
- backend/ (phase 1)
- docs/ (phase 3)
- .github/workflows/
- scripts/
- tests/fixtures/

## Verify
```bash
cd /home/ubuntu/gallery-repo/frontend
pnpm run lint
pnpm run build
pnpm run test:unit 2>&1 | tail -5
cd /home/ubuntu/gallery-repo/backend
python -m pytest -q tests/ --tb=short 2>&1 | tail -3
```

## Commit + push
Commit message: `refactor: clean frontend stale API references and dead code`
