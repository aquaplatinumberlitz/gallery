Rename catalog/ package theo Option A. Repo: /home/ubuntu/gallery-repo, branch: main.

## Cấu trúc hiện tại
```
backend/catalog/
├── __init__.py              # 1 dòng docstring
├── service.py               # 409 dòng — background worker (scan/rebuild queue)
└── status_builder.py        # 985 dòng — catalog status query builder
```

## Cấu trúc mới
```
backend/
├── scan_worker.py           # từ catalog/service.py
├── status_builder.py        # từ catalog/status_builder.py
```

Xoá: `backend/catalog/` (sau khi move xong)

## Files cần move

### Step 1: git mv
```bash
git mv backend/catalog/service.py backend/scan_worker.py
git mv backend/catalog/status_builder.py backend/status_builder.py
git rm backend/catalog/__init__.py
# rmdir backend/catalog/ (nếu còn)
```

### Step 2: Sửa imports

**backend/app.py:**
- `from .catalog import service as catalog_service` → `from .scan_worker import start, stop, queue_startup_scans`
- `catalog_service.start()` → `start()`
- `catalog_service.stop()` → `stop()`
- `catalog_service.queue_startup_scans(...)` → `queue_startup_scans(...)`

**backend/libraries.py:**
- `from .catalog import service as catalog_service` → `from .scan_worker import queue_scan, queue_rebuild`
- `from .catalog.status_builder import ...` → `from .status_builder import ...`
- `catalog_service.queue_rebuild(...)` → `queue_rebuild(...)`
- `catalog_service.queue_scan(...)` → `queue_scan(...)`

**backend/refresh.py:**
- `from .catalog.service import queue_scan` → `from .scan_worker import queue_scan`

**backend/watcher.py:**
- `from .catalog.service import queue_watcher_scan` → `from .scan_worker import queue_watcher_scan`

**backend/tests/conftest.py:**
- `from backend.catalog import service as catalog_service` → `from backend.scan_worker import stop as stop_scan_worker, start, queue_startup_scans`
- `catalog_service.stop()` → `stop_scan_worker()`
- `monkeypatch.setattr("backend.app.catalog_service.start", _noop)` → `monkeypatch.setattr("backend.scan_worker.start", _noop)`
- `monkeypatch.setattr("backend.app.catalog_service.queue_startup_scans", lambda: [])` → `monkeypatch.setattr("backend.scan_worker.queue_startup_scans", lambda: [])`

**backend/tests/test_libraries_catalog.py:**
- `from backend.catalog import service as catalog_service` → `from backend import scan_worker as catalog_service`
- (giữ alias catalog_service để ko phải sửa từng dòng)

**backend/tests/test_library_jobs.py:**
- `from backend.catalog import service as catalog_service` → `from backend import scan_worker as catalog_service`

**backend/tests/test_catalog_trigger_routing.py:**
- `from backend.catalog import service as catalog_service` → `from backend import scan_worker as catalog_service`
- `import backend.catalog.service as svc` → `import backend.scan_worker as svc`

**backend/tests/test_libraries_coverage.py:**
- `from backend.catalog import service as catalog_service` → `from backend import scan_worker as catalog_service`

**backend/tests/test_catalog_status_contract.py:**
- `from backend.catalog.status_builder import PrecedenceFacts, derive_summary_state` → `from backend.status_builder import PrecedenceFacts, derive_summary_state`

**backend/tests/test_catalog_status_endpoints.py:**
- Check imports, update nếu cần.

**backend/tests/test_browse_api.py:**
- string "catalog" trong test data (ko phải import) — giữ nguyên

Các file test docstring có "catalog" — giữ nguyên.

### Step 3: Verify
```bash
cd /home/ubuntu/gallery-repo/backend
python -m pytest -q tests/ --tb=short 2>&1 | tail -5
ruff check backend/
```

### Step 4: Commit + push
```bash
git add -A
git commit -m "refactor: rename catalog/ to scan_worker.py + status_builder.py"
git push origin main
```

## KHÔNG được sửa
- Nội dung logic của scan_worker.py và status_builder.py (chỉ move file + sửa import)
- backend/metadata_store/ (ko liên quan)
- tests/fixtures/catalog_status/ (giữ nguyên)
- String references "catalog" trong test assertions/docstrings (giữ nguyên)