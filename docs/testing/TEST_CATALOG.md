# Test Catalog

Status: Maintained catalog

Last reviewed: 2026-06-26

Catalog entries summarize the regression or contract each important test file
protects. File-level headers in tests provide the same purpose fields close to
the code.

| File | Type | Purpose | Guarantees | Run when | Related feature |
| --- | --- | --- | --- | --- | --- |
| `backend/tests/test_albums.py` | Backend unit | Album helper filesystem edge cases. | Album metadata tolerates hidden dirs, stat errors, and unreadable dirs. | Changing album helper behavior. | Albums |
| `backend/tests/test_api_integration_derivatives.py` | Backend integration | Image, thumbnail, and preview endpoint contracts. | Derivative sizing, cache headers, and endpoint separation stay stable. | Changing image serving or derivative cache keys. | Image APIs |
| `backend/tests/test_api_integration_health_and_safety.py` | Backend integration | Health and public API path safety. | Unsafe paths are rejected across image, thumbnail, preview, and metadata endpoints. | Changing path safety or health fields. | Health/safety |
| `backend/tests/test_api_integration_metadata_search_facets.py` | Backend integration | Metadata, search, fielded query, and facets endpoints. | Indexed-image response shapes and filters stay stable. | Changing metadata parsing, search SQL, or facets aggregation. | Metadata/search/facets |
| `backend/tests/test_app.py` | Backend broad regression | Route registration, health, unsafe paths, and broad fielded search semantics. | App routes and scoped/CJK/fielded search behavior remain intact. | Changing app routes or search integration. | App/search |
| `backend/tests/test_app_coverage.py` | Backend coverage | Metrics, profiler, and CORS setup branches. | Optional middleware and CORS behavior remain covered. | Changing app setup. | App setup |
| `backend/tests/test_asset_reactivate.py` | Backend regression | Asset reactivation behavior for metadata and catalog writes. | Metadata upserts do not reactivate offline assets; explicit indexing can. | Changing asset upsert or offline handling. | Asset lifecycle |
| `backend/tests/test_browse_api.py` | Backend integration | `/api/browse` virtual-root, folder, pagination, availability, and scope behavior. | Browse stays catalog-backed, read-only, cursor-compatible, and library-scoped. | Changing browse response shape or catalog listing. | Browse API |
| `backend/tests/test_catalog_status_contract.py` | Backend contract | Unified catalog status fixtures, JSON schema, and summary precedence. | Contract v1 stays schema-valid and serializable across summary states. | Changing status schema or fixtures. | Catalog Status API |
| `backend/tests/test_catalog_status_endpoints.py` | Backend integration | `/api/libraries/{id}/status` and `/api/libraries/status`. | Status envelopes include scoped counts, availability, runtime, and errors. | Changing status endpoints or aggregation SQL. | Catalog Status API |
| `backend/tests/test_catalog_recovery.py` | Backend unit | Recovery of stale catalog jobs after server restart. | Running scan/rebuild jobs are failed with timestamps and message; terminal/queued/non-catalog jobs untouched; recovery idempotent; queued jobs unblocked after recovery; worker startup runs recovery before claim. | Changing recover_stale_jobs, claim_next_catalog_job, or scan_worker.start(). | Catalog job lifecycle |
| `backend/tests/test_catalog_status_ready_assets.py` | Backend integration | Ready-asset aggregation in single and batch status. | Ready counts require current image metadata and match across status surfaces. | Changing metadata freshness joins. | Catalog Status API |
| `backend/tests/test_catalog_status_mtime_tolerance.py` | Backend regression | Tolerant mtime_ns identity joins for catalog status counts and issues. | Ready, queued, running, failed, and batch status paths all match assets using the tolerant identity rule. | Changing metadata freshness joins or status identity rules. | Catalog Status API |
| `backend/tests/test_catalog_trigger_routing.py` | Backend integration | Durable catalog scan/rebuild job routing and conflict behavior. | Manual, watcher, startup, scheduled, and rebuild jobs queue predictably. | Changing catalog service queueing. | Catalog jobs |
| `backend/tests/test_db_path_consistency.py` | Backend regression | Metadata DB path source of truth. | `_connect()` and `initialize_database()` use the same configured DB path. | Changing DB path resolution. | Metadata DB config |
| `backend/tests/test_derivative_scheduler.py` | Backend integration | Durable derivative scheduler lifecycle and catalog status integration. | Warm/clear flows coalesce jobs and report derivative coverage correctly. | Changing derivative scheduling or variants. | Derivative jobs |
| `backend/tests/test_derivatives.py` | Backend unit/integration | Derivative rendering, cache versioning, EXIF dimensions, and failures. | Thumbnail/preview sizing, no-upscale, and EXIF orientation stay stable. | Changing derivative generation. | Derivatives |
| `backend/tests/test_facets.py` | Backend unit/integration | DB-backed facet aggregation. | Facets do not scan the filesystem and respect scope/output limits. | Changing facets SQL or LoRA facets. | Facets |
| `backend/tests/test_fielded_search_parser.py` | Backend unit | Fielded query parsing and SQL builder behavior. | Field aliases, comparisons, quoted values, and residual terms remain stable. | Changing parser syntax. | Fielded search |
| `backend/tests/test_folders.py` | Backend integration | Folder listing and open-folder route edge cases. | Folder APIs handle hidden/excluded dirs and filesystem errors safely. | Changing folder routes or ignore logic. | Folders |
| `backend/tests/test_identity_helper.py` | Backend unit | Shared metadata identity SQL fragments and tolerance constants. | Asset/metadata/job matching keeps canonical tolerant `mtime_ns` behavior, legacy seconds bridge, alias substitution, and exported tolerance constants. | Changing metadata identity predicates or freshness joins. | Metadata identity |
| `backend/tests/test_indexer_staging.py` | Backend unit/integration | Metadata indexer staging, dedupe, flush, retry, and scoped runtime counters. | Scan-time enqueueing and runtime attribution stay stable. | Changing metadata indexer queues. | Metadata indexer |
| `backend/tests/test_integrity_checker_contract.py` | Backend contract | IntegrityChecker per-check behavior, file-health summary mapping, run persistence, and schema-check edge cases. | Checks repair only intended rows; `run_all_checks` exposes the expected eight keys; persisted summaries map to five issue and four repair keys. | Changing integrity checker checks, maintenance run mapping, or schema_check behavior. | Integrity checker |
| `backend/tests/test_libraries_catalog.py` | Backend integration | Library registration, catalog asset writes, migrations, and management API behavior. | Libraries, import paths, file index rows, and asset rows stay consistent. | Changing library CRUD or catalog writes. | Library catalog |
| `backend/tests/test_libraries_coverage.py` | Backend coverage | Edge/error branches in `backend/libraries.py`. | Validation, scan/rebuild, derivative, event, and error branches remain covered. | Changing library route helpers. | Library management backend |
| `backend/tests/test_library_inspector.py` | Backend integration | Library Inspector scope, search, detail, exclusion, and fallback contracts. | Inspector semantics do not alter normal search behavior. | Changing inspector endpoints. | Library Inspector backend |
| `backend/tests/test_library_jobs.py` | Backend integration | Durable job state, scan-all, stats, stale recovery, and SSE formatting. | Job counters, parent/child relationships, and event payloads remain stable. | Changing library job storage or SSE events. | Library jobs |
| `backend/tests/test_maintenance_file_health_api.py` | Backend integration | Maintenance file-health API router contract. | Never-run, latest-run, manual-check, daemon-run, failed-run, and concurrent-run responses keep the stable Maintenance page envelope. | Changing maintenance routes, file-health response models, or IntegrityChecker run persistence. | Maintenance file health |
| `backend/tests/test_metadata_binary_sanitizer.py` | Backend unit | Binary metadata sanitization. | Binary PIL fields do not break JSON storage. | Changing metadata extraction or sanitizer logic. | Metadata parsing |
| `backend/tests/test_metadata_extract_parsers.py` | Backend unit | A1111, ComfyUI, SwarmUI, NovelAI, EasyDiffusion parser dispatch. | Parsers handle valid, invalid, and malformed inputs safely. | Changing metadata parser logic. | Metadata extraction |
| `backend/tests/test_metadata_parse_coverage.py` | Backend coverage | Metadata route/cache/error branches. | In-flight parse sharing and API error mapping remain covered. | Changing metadata route cache or parser dispatch. | Metadata API |
| `backend/tests/test_metadata_store_coverage.py` | Backend coverage | Metadata store helper branches. | Folder state, dimensions, jobs, cleanup, and initialization helpers stay covered. | Changing low-level DB helpers. | Metadata store |
| `backend/tests/test_metadata_store_search.py` | Backend unit | Metadata search FTS, trigram, LIKE fallback, and empty query behavior. | Search works when FTS is unavailable and chooses fallback paths correctly. | Changing metadata search SQL. | Metadata search |
| `backend/tests/test_mixed_media_listing.py` | Backend integration | Mixed image/video listing and filename search grouping. | Image/video asset types and video search sections stay canonical. | Changing asset type normalization or search grouping. | Mixed media backend |
| `backend/tests/test_paths.py` | Backend unit | `resolve_path` and `is_path_safe` edge cases. | Path safety stays rooted and resolve exceptions fail closed. | Changing path resolution. | Path safety |
| `backend/tests/test_refresh_service.py` | Backend unit | Scheduled refresh worker ticks and lifecycle. | Refresh respects roots, max work, stop events, and idempotent start/stop. | Changing refresh logic. | Scheduled refresh |
| `backend/tests/test_scan_folder_counts.py` | Backend unit | Refresh folder counts and warm listing count avoidance. | Folder/image/child counts are accurate for refresh state. | Changing `_scan_folder_counts`. | Refresh/warm listing |
| `backend/tests/test_scheduled_refresh.py` | Backend integration | Scheduled refresh config, work limits, and safe fallbacks. | Refresh queues scheduled catalog scans and handles busy/error states safely. | Changing refresh config or loop. | Scheduled refresh |
| `backend/tests/test_schema_check.py` | Backend unit | Catalog lifecycle schema-check helper. | Fresh catalog DBs pass; missing lifecycle tables, columns, and indexes report stable issue strings. | Changing catalog schema requirements or additive migrations. | Schema check |
| `backend/tests/test_search_coverage.py` | Backend coverage | Search and inspector error branches. | Unsafe/missing paths, invalid cursors, and backend failures map to stable API errors. | Changing search route errors. | Search/inspector |
| `backend/tests/test_static_files.py` | Backend integration | Static file serving, landing pages, and SPA fallback. | Dev/prod root behavior and catch-all routing remain stable. | Changing static routes. | Static file serving |
| `backend/tests/test_thumbnails_coverage.py` | Backend coverage | Thumbnail helper branches and API error mapping. | Derivative edge cases and cache responses remain covered. | Changing thumbnail rendering or ETags. | Thumbnails |
| `backend/tests/test_video.py` | Backend integration | Video indexing, byte-range streaming, poster generation, and stats. | Video routes stream correctly and report stable poster/stats behavior. | Changing video routes or media classification. | Video |
| `backend/tests/test_warm_folder_listing.py` | Backend integration | SQLite-backed warm folder listing and fallback paths. | Warm listings avoid hot-path `scandir` work and match catalog listing behavior. | Changing warm listing SQL. | Warm listing |
| `backend/tests/test_watcher.py` | Backend unit | Watcher config, debounce, and catalog scan hooks. | Watcher starts safely, filters roots, and queues catalog scans predictably. | Changing watcher debounce or event handling. | File watcher |
| `frontend/tests/e2e/active-library-selection.spec.ts` | Frontend contract | Registered-library selection, legacy raw path migration, and no-library states. | Gallery persists registered IDs and removes legacy raw path state. | Changing active library selection. | Active library selection |
| `frontend/tests/e2e/advanced-search-drawer.spec.ts` | Frontend contract | Advanced drawer field controls and result rendering. | Drawer controls emit expected fielded query strings. | Changing advanced search UI. | Advanced search |
| `frontend/tests/e2e/breadcrumb.spec.ts` | Frontend contract | Deep breadcrumb rendering and navigation. | Long paths and breadcrumb clicks preserve SPA navigation behavior. | Changing breadcrumb path handling. | Breadcrumb |
| `frontend/tests/e2e/fault-injection.spec.ts` | Frontend resilience contract | Controlled fallback UI for failed endpoints and offline states. | API failures do not crash gallery, metadata, thumbnails, previews, or lightbox. | Changing error handling. | Fault tolerance |
| `frontend/tests/e2e/gallery-cache-revisit.spec.ts` | Frontend cache contract | Soft revisits and browser-back cache reuse. | Album data survives revisits without duplicate cursor=0 browse requests. | Changing query keys or cache lifetime. | Query/cache |
| `frontend/tests/e2e/gallery-no-reload-real-backend.spec.ts` | Frontend real-backend smoke | No-reload checks with a real backend. | Real responses do not cause unexpected full page reloads. | Investigating reloads with real data. | Real-backend navigation |
| `frontend/tests/e2e/gallery-no-reload.spec.ts` | Frontend contract | No-reload SPA album and lightbox navigation. | Boot identity stays stable and browse requests are not duplicated. | Changing router or navigation. | SPA navigation |
| `frontend/tests/e2e/index-rebuild-flow.spec.ts` | Frontend diagnostic/contract | Rebuild timing, stale notice behavior, invalidation, and debug logs. | Rebuild refreshes inspector/status data and emits useful diagnostics. | Changing rebuild invalidation. | Rebuild/index convergence |
| `frontend/tests/e2e/index-status-panel.spec.ts` | Frontend contract | Catalog status copy, scoped/global separation, details, and actions. | Current-scope status remains primary and global work stays secondary. | Changing `IndexStatusPanel.vue`. | Catalog Status |
| `frontend/tests/e2e/library-inspector.spec.ts` | Frontend contract | Library Inspector route, table, detail, search, sort, and lightbox order. | Inspector UI reflects API responses and hides debug/internal fields by default. | Changing inspector UI or hooks. | Library Inspector |
| `frontend/tests/e2e/library-management.spec.ts` | Frontend contract | Responsive library list/detail flows and management actions. | CRUD, scan, rebuild, scan-all, and confirmations call expected endpoints. | Changing library management UI. | Library management |
| `frontend/tests/e2e/lightbox-loading-policy.spec.ts` | Frontend contract | Thumbnail/preview/original endpoint policy. | Normal lightbox open avoids original image fetches; escalation is explicit. | Changing PhotoSwipe source policy. | Lightbox source policy |
| `frontend/tests/e2e/lightbox-visual-layer.spec.ts` | Frontend visual contract | PhotoSwipe roots, visible images, reopen lifecycle, and EXIF portrait rendering. | No duplicate visible PhotoSwipe images or broken rotated dimensions. | Changing lightbox layers or CSS. | PhotoSwipe/lightbox |
| `frontend/tests/e2e/metadata-performance.spec.ts` | Frontend perf diagnostic/gate | `/metadata` navigation, API timing, render/update timing, debounce, and state restore. | Timing JSON and optional budgets catch inspector performance regressions. | Diagnosing metadata route performance. | Library Inspector performance |
| `frontend/tests/e2e/mixed-media-gallery.spec.ts` | Frontend contract | Mixed image/video gallery rendering and media-specific open behavior. | Images open in lightbox; videos open in the video dialog. | Changing mixed-media gallery rendering. | Mixed media gallery |
| `frontend/tests/e2e/mobile-lightbox-sheet.spec.ts` | Frontend responsive contract | Mobile gallery controls, lightbox metadata sheet, and mobile search. | Mobile metadata sheet opens/closes repeatedly without trapping the UI. | Changing mobile lightbox or mobile search. | Mobile lightbox |
| `frontend/tests/e2e/perf/album-open.perf.spec.ts` | Frontend perf smoke | Album open browse and thumbnail timing. | Perf JSON captures post-click browse/thumbnail timing against budgets. | Validating album open performance. | Performance |
| `frontend/tests/e2e/perf/lightbox.perf.spec.ts` | Frontend perf smoke | Lightbox open and next-image transition timing. | Preview/original endpoint timing is visible against lightbox budgets. | Validating lightbox performance. | Performance |
| `frontend/tests/e2e/responsive-breakpoints.spec.ts` | Frontend responsive contract | Mobile, tablet, desktop, large desktop, and layout transitions. | Layout shells switch cleanly at supported viewport widths. | Changing breakpoints or layout components. | Responsive layout |
| `frontend/tests/e2e/search-fielded-ui.spec.ts` | Frontend contract | Main gallery plain and fielded search. | Queries, results, clearing, no-results, and special chars stay stable. | Changing search inputs. | Search UI |
| `frontend/tests/e2e/settings-modal.spec.ts` | Frontend contract | Settings persistence and landing-page choices. | Intro, theme, and original-loading preferences persist correctly. | Changing settings modal. | Settings |
| `frontend/tests/e2e/sidebar-trigger.spec.ts` | Frontend responsive contract | Sidebar trigger visibility and behavior. | Sidebar controls stay discoverable without unexpected duplicates. | Changing sidebar state or trigger UI. | Sidebar |
| `frontend/tests/e2e/tailwind-phase0.spec.ts` | Frontend styling regression | Tailwind phase 0 visual contracts. | Utility migration keeps desktop/mobile/tablet/theme baselines stable. | Changing Tailwind setup or token bridge CSS. | Tailwind migration |
| `frontend/tests/e2e/tailwind-preflight.spec.ts` | Frontend styling regression | Tailwind preflight/global CSS regression behavior. | Base component styling is not erased by global CSS changes. | Changing global CSS order or preflight. | Styling |
| `frontend/src/components/__tests__/IndexStatusDetailsPopover.test.ts` | Frontend component unit | Status popover badges, issue copy, global-work notes, and contract error display. | Status detail copy stays stable across edge states. | Changing status details popover. | Catalog Status UI |
| `frontend/src/components/__tests__/VideoCard.test.ts` | Frontend component unit | Video card poster URL, play affordance, duration, and poster fallback. | Video cards remain stable when poster generation succeeds or fails. | Changing `VideoCard.vue`. | Video card |
| `frontend/src/composables/__tests__/useClipboard.test.ts` | Frontend unit | Modern Clipboard API path, labels, errors, and reset timer. | Copy status remains stable. | Changing clipboard composable. | Clipboard |
| `frontend/src/composables/__tests__/useColumnResize.test.ts` | Frontend unit | Responsive grid slider mapping, persistence, migration, and row-height recompute. | Column levels map per device and migrate legacy counts. | Changing grid density. | Column resize |
| `frontend/src/composables/__tests__/useDelayedBoolean.test.ts` | Frontend unit | Timer-based boolean cancellation, rescheduling, and unmount cleanup. | Delayed state flips only when expected and timers are cleaned up. | Changing delayed boolean logic. | Delayed boolean |
| `frontend/src/composables/__tests__/useDevice.test.ts` | Frontend unit | Device breakpoint detection and reactive screen-size classes. | Breakpoint values match viewport width at registration time. | Changing device breakpoint thresholds. | Device breakpoints |
| `frontend/src/components/__tests__/Lightbox.test.ts` | Frontend unit | FocusScope focus trapping (Tab/Shift+Tab wrap, focus restore on unmount). | FocusScope traps Tab/Shift+Tab while open and restores focus on unmount. | Changing focus scope behavior. | Focus scope |
| `frontend/src/composables/__tests__/useGalleryTheme.test.ts` | Frontend unit | Theme mode persistence, transitions, and reduced-motion fallback. | Theme mode and resolved theme update consistently. | Changing theme composable. | Gallery theme |
| `frontend/src/composables/__tests__/useHaptic.test.ts` | Frontend unit | `navigator.vibrate` guards and light/medium patterns. | Haptic helpers do not throw when vibrate is unavailable. | Changing haptic wrappers. | Haptic feedback |
| `frontend/src/composables/__tests__/useNaturalSort.test.ts` | Frontend unit | Natural-sort key generation and comparator. | Numeric and text chunks sort in natural order. | Changing natural sort. | Natural sort |
| `frontend/src/composables/__tests__/usePullToRefresh.test.ts` | Frontend unit | Pull-to-refresh gesture state machine and haptic feedback. | Axis lock, threshold, max distance, and refresh guard remain stable. | Changing pull-to-refresh. | Pull-to-refresh |
| `frontend/src/composables/__tests__/useScrollVisibility.test.ts` | Frontend unit | Scroll-driven bar visibility. | Down-hide/up-show, bottom guard, attach paths, and cleanup remain stable. | Changing scroll visibility. | Scroll visibility |
| `frontend/src/composables/__tests__/useToast.test.ts` | Frontend unit | Toast composable wrappers and promise flow. | Convenience wrappers delegate to the store correctly. | Changing toast composable. | Toast composable |
| `frontend/src/composables/admin/__tests__/useGeneratedImagesStatusQuery.test.ts` | Frontend unit | Generated-image status query enablement and API call shape. | Query runs only with a valid library id and calls the expected API wrapper. | Changing generated-image status queries. | Generated images admin |
| `frontend/src/composables/admin/__tests__/useGeneratedImagesMutations.test.ts` | Frontend unit | Library-scoped generated-image warm mutation. | Warm calls the library-scoped API and invalidates generated-image, status, job, and browse keys. | Changing generated-image warm behavior. | Generated images admin |
| `frontend/src/composables/admin/__tests__/useGeneratedImagesGlobalMutations.test.ts` | Frontend unit | All-library generated-image refresh and clear mutations. | Refresh and clear call global APIs and invalidate generated-image, status, job, and browse keys. | Changing global generated-image maintenance actions. | Generated images admin |
| `frontend/src/composables/admin/__tests__/useFileHealthQuery.test.ts` | Frontend unit | Maintenance file-health query and mutation composable. | Fetch and manual-check calls use the file-health API and invalidate the maintenance file-health query on success. | Changing Maintenance page file-health data flow or query keys. | Maintenance file health |
| `frontend/src/composables/admin/__tests__/useLibraryEvents.test.ts` | Frontend unit | EventSource lifecycle, named event parsing, invalid JSON, and unavailable EventSource behavior. | Library event connection state and invalidation behavior stay stable. | Changing SSE composable. | Library events |
| `frontend/src/composables/admin/__tests__/useLibraryMutations.test.ts` | Frontend unit | Query invalidation after library mutations. | Mutations invalidate expected library, status, browse, stats, and job keys. | Changing mutation composables. | Library mutations |
| `frontend/src/contracts/__tests__/catalogStatusContract.test.ts` | Frontend contract unit | Frontend catalog-status fixtures and schema expectations. | Frontend contract types stay aligned with backend status fixtures. | Changing shared status fixtures or types. | Catalog Status contract |
| `frontend/src/contracts/__tests__/maintenanceFileHealthContract.test.ts` | Frontend contract unit | Maintenance file-health fixtures and JSON schema expectations. | Frontend file-health types stay aligned with backend fixtures, exact issue keys, exact repair keys, and the concurrent-run envelope. | Changing maintenance file-health schema, fixtures, or frontend types. | Maintenance file health contract |
| `frontend/src/lib/catalog/__tests__/contractGuard.test.ts` | Frontend unit | Runtime guards for status envelopes and batch responses. | Valid contract-v1 payloads pass and invalid payloads throw. | Changing status guards. | Catalog Status contract |
| `frontend/src/lib/catalog/__tests__/labels.test.ts` | Frontend unit | Status label and presentation mapping. | Labels, tones, pulse behavior, and fallbacks stay stable. | Changing status presentation copy. | Catalog Status labels |
| `frontend/src/lib/catalog/__tests__/polling.test.ts` | Frontend unit | Active-status detection and refetch interval policy. | Status polling cadence follows settled vs active work state. | Changing polling policy. | Catalog Status polling |
| `frontend/src/query/__tests__/keys.test.ts` | Frontend unit | Query-key factories for library, browse, status, jobs, and inspector keys. | Query keys preserve normalization and cache partition fields. | Changing query keys or invalidation scopes. | Query keys |
| `frontend/src/services/__tests__/api.test.ts` | Frontend unit | `GalleryAPIError.fromAxiosError` error-type parsing and URL helper functions. | All error types (network, timeout, library, not_found, bad_request, permission, invalid_file, confirmation_required, fallback) parsed correctly from FastAPI detail wrappers; URL helpers build correct endpoints. | Changing API error handling or URL helper logic. | API client |
| `frontend/src/stores/__tests__/gallery.test.ts` | Frontend unit | Gallery store active library lifecycle, navigation, sort, expansion, and explorer errors. | Store actions produce expected state and toast transitions. | Changing gallery store. | Gallery store |
| `frontend/src/stores/__tests__/lightbox.test.ts` | Frontend unit | Lightbox open/close, image filtering, index resolution, preloading, and dimensions. | Navigation and dimension memory remain stable. | Changing lightbox store. | Lightbox store |
| `frontend/src/stores/__tests__/toast.test.ts` | Frontend unit | Toast store adapts Gallery API to Sonner: IDs, variants, durations, dismiss, clear, actions, and visible-toast limit. | Gallery toast API remains stable while Sonner owns render/dismiss mechanics. | Changing toast store. | Toast store |
| `frontend/src/utils/__tests__/assetType.test.ts` | Frontend unit | Asset type normalization and media checks. | Legacy `photo`/`file` values normalize to image and media checks stay stable. | Changing asset type vocabulary. | Asset type helpers |
| `frontend/src/utils/__tests__/fuzzySearch.test.ts` | Frontend unit | Fuse-backed fuzzy FileNode matching. | Empty queries, includePath, cache reuse, special chars, and CJK stay stable. | Changing fuzzy search. | Fuzzy search |
| `frontend/src/utils/__tests__/format.test.ts` | Frontend unit | `formatBytes` formatting, edge cases (`NaN`, `Infinity`, negative), and helper signature. | Invalid inputs return `"—"`, zero returns `"0 B"`, valid sizes format correctly. | Changing formatting logic. | Utility formatting |
| `frontend/src/utils/__tests__/gallery.test.ts` | Frontend unit | Infinite scroll load-more guard contract via `shouldLoadMoreImages`. | All four guards (hasMoreImages, isLoadingMore, isFetching, hasSearchQuery) block correctly individually and combined. | Changing `shouldLoadMoreImages` guard logic. | Infinite scroll guards |
| `frontend/src/utils/__tests__/infiniteScroll.int.test.ts` | Frontend unit/integration | Infinite scroll observer callback and load-more guard contract. | Intersecting sentinel calls fetchNextPage only when pagination, loading, fetching, and search guards allow it. | Changing GalleryGrid infinite scroll, observer callback, or load-more guard logic. | Infinite scroll |
| `frontend/src/utils/__tests__/indexMaintenance.test.ts` | Frontend unit | Scope rebuild marker mark/get/clear behavior. | Markers propagate and clear only across correct path boundaries. | Changing rebuild invalidation markers. | Index rebuild markers |
| `frontend/src/utils/__tests__/lightbox.test.ts` | Frontend unit | PhotoSwipe item building, dimensions, animated detection, and original-load preference. | Item URLs and fallback dimensions remain stable. | Changing lightbox helpers. | Lightbox helpers |
| `frontend/src/utils/__tests__/loraHighlighter.test.ts` | Frontend unit | `<lora:...>` tokenization and escaping. | Lora pill rendering and HTML escaping remain stable. | Changing LoRA highlighter. | Lora highlighter |
| `frontend/src/utils/__tests__/serializeAdvancedSearchToQuery.test.ts` | Frontend unit | Fielded search serialization, display strings, parser, and round-trips. | Quoting, escaping, operators, and literal fields remain stable. | Changing advanced search syntax. | Advanced search |
| `scripts/audit_test_matrix.py` | Test audit helper | Collects pytest, Playwright, and Vitest test ids and compares docs. | Reports catalog drift, matrix gaps, coverage gaps, and perf reports. | Auditing test coverage. | Test gap audit |
| `scripts/create_perf_fixture.py` | Perf fixture helper | Creates deterministic gallery tree and metadata DB. | Repeatable perf smoke data supports browse, thumbnails, lightbox, and inspector. | Preparing local/CI perf data. | Performance fixtures |
| `scripts/internal/perf-smoke.sh` | Internal perf runner | Implements `./test.sh perf-smoke`. | Gated perf checks run against real data or deterministic fixture. | Before release or perf-sensitive changes. | Performance |
| `scripts/perf_lib.py` | Perf helper | Shared perf budget loading and summary helpers. | Python perf scripts read canonical TOML budgets consistently. | Changing perf budget format. | Performance helpers |
| `scripts/perf_library_inspector.py` | Backend local perf smoke | Direct Library Inspector list/query latency. | JSON includes timings and fails on p95/min-row regressions. | Changing inspector SQL or budgets. | Library Inspector performance |
| `scripts/perf_warm_listing.py` | Backend local perf smoke | Cold filesystem vs warm SQLite listing timing. | Warm listing duration is reported and budgeted. | Changing warm listing SQL. | Warm listing performance |
| `scripts/summarize_perf_reports.py` | Perf reporting helper | Aggregates perf report JSON files. | Writes `perf-summary.json` and `perf-summary.md`. | After perf smoke tests. | Performance reporting |
| `backend/tests/test_scan_worker.py` | Backend characterization (Phase 0A) | Bug 1: rebuild schedules DB jobs but does not dispatch runtime metadata work. | `execute_rebuild_job` creates `metadata_index_jobs` rows but `_job_queue` stays empty. | Touching rebuild/index tests or metadata job scheduling. | Metadata lifecycle |
| `backend/tests/test_indexer_staging.py` | Backend characterization (Phase 0A) | Bug 1 divergence: scan path calls `_enqueue_metadata_jobs_from_result` but rebuild path does not. | Scan and rebuild share the same durable scheduling entrypoint after D full clean. | Changing metadata indexer queues or scheduling. | Metadata lifecycle |
| `backend/tests/test_indexer_staging.py` | Backend characterization (Phase 0A) | `queue_metadata_index_paths` is idempotent and coalesces duplicate calls. | Repeated dispatch does not create duplicate work. | Changing queue coalesce policy. | Metadata lifecycle |
| `backend/tests/test_metadata_store_coverage.py` | Backend characterization (Phase 0A) | Bug 2: "already current" shortcut marks `metadata_index_jobs.state='done'` but does not update `assets.metadata_state`. | Shortcut must materialize both job-done and asset-done. | Changing `queue_metadata_index_paths` coalesce/skip/fail policy. | Metadata lifecycle |
| `backend/tests/test_metadata_store_coverage.py` | Backend characterization (Phase 0A) | `mark_metadata_jobs_done` has no DB-side stale guard against changed `mtime_ns`/`size`. | Stale jobs must not mark changed assets done. | Adding completion stale guards. | Metadata lifecycle |
| `backend/tests/test_catalog_status_ready_assets.py` | Backend characterization (Phase 0A) | `ready_assets` requires both `assets.metadata_state='done'` AND current `image_metadata`; job table alone cannot make an asset ready. | Status API correctly reads materialized asset state. | Changing metadata freshness joins. | Catalog Status API |
| `backend/tests/test_metadata_lifecycle.py` | Backend contract (Phase 0B) | DB-claim worker claims jobs directly from SQLite. | `claim_next_metadata_job` reads from `metadata_index_jobs`, not from in-memory queue. | Changing claim/complete primitives or worker lifecycle. | Metadata lifecycle |
| `backend/tests/test_metadata_lifecycle.py` | Backend contract (Phase 0B) | Claimed jobs move `queued -> running` atomically. | No two callers can claim the same job. | Changing claim atomicity. | Metadata lifecycle |
| `backend/tests/test_metadata_lifecycle.py` | Backend contract (Phase 0B) | Worker does not hold long write transactions during extraction. | Extract runs outside claim/complete transactions. | Changing worker loop or transaction scope. | Metadata lifecycle |
| `backend/tests/test_metadata_lifecycle.py` | Backend regression (Phase 6) | `complete_metadata_job` marks skipped when no asset row exists. | No asset → skipped; asset version mismatch → stale. | Changing completion gating logic. | Metadata lifecycle |
| `backend/tests/test_metadata_lifecycle.py` | Backend regression (Phase 6) | Priority is persisted and claim picks lower numeric first. | `dispatch_metadata_index_paths(priority=1)` claimed before priority=3. | Changing claim ordering. | Metadata lifecycle |
| `backend/tests/test_metadata_lifecycle.py` | Backend regression (Phase 6) | Recovery handles >1000 running jobs and fails exhausted attempts. | Unbounded recovery; attempts >= MAX_METADATA_JOB_ATTEMPTS become failed. | Changing recovery logic. | Metadata lifecycle |
| `backend/tests/test_metadata_lifecycle.py` | Backend regression (Phase 6) | Legacy `image_metadata.mtime_ns=NULL` + matching `mtime`/`size` results in job done and asset done. | Legacy matching works through `_persist_metadata_index_jobs` shortcut. | Changing legacy matching logic. | Metadata lifecycle |
| `backend/tests/test_integrity_checker.py` | Backend contract (Phase 7+) | IntegrityChecker cross-table consistency checks for assets, metadata jobs, derivatives, and persisted file-health runs. | Check result keys, repair/requeue/fail behavior, daemon/manual run persistence, and lifecycle thread-safety stay stable. | Changing integrity check logic, derivative job scheduling, or maintenance run persistence. | Integrity checker |
| `backend/tests/test_metadata_lifecycle.py` | Backend regression (Phase 6) | `get_metadata_lifecycle_status` returns 15 counters including inconsistency, worker health, and throughput. | Diagnostics reflect current DB state and worker status. | Changing lifecycle diagnostics. | Metadata lifecycle |
| `backend/tests/test_library_events_coverage.py` | Backend coverage | SSE subscriber lifecycle, event publishing, and streaming branches. | publish handles queue full, closed loops; event_stream yields keepalive and cleans up on disconnect. | Changing library_events.py subscriber lifecycle or event_stream async generator. | SSE events |
| `backend/tests/test_folder_index_coverage.py` | Backend coverage | Warm folder listing and scan-result indexing error/edge-case branches. | Exception handling in update/get folder state; warm listing guards; scan folder counts skip dot-files. | Changing folder_index.py exception handling or index_files_from_scan error paths. | Folder index |
| `backend/tests/test_indexer_coverage.py` | Backend coverage | Metric helpers, path utilities, and MetadataLifecycleWorker exception branches. | Worker continues after claim exception; _is_job_current handles missing files and legacy mtime. | Changing indexer.py metric/path helpers or worker exception handling. | Metadata indexer |
| `frontend/src/utils/__tests__/assetType.test.ts` | Frontend unit | Asset type normalization and media-type detection. | normalizeAssetType maps legacy photo/file to image; isMediaAssetType returns correct booleans. | Changing asset type normalization. | Asset types |
| `frontend/src/utils/__tests__/libraryStatus.test.ts` | Frontend unit | Timestamp and asset-count formatting for status display. | formatLibraryTimestamp returns Never/Unknown for null/invalid; formatAssetCount handles zero/null/negative. | Changing status display formatting. | Status display |
| `frontend/src/lib/catalog/__tests__/status.test.ts` | Frontend unit | Catalog status summary-state derivation. | deriveSummaryState returns correct state for all 12 precedence branches (unknown, offline, scanning, indexing, error, needs_scan, needs_update, ready_with_issues, ready). | Changing status derivation logic. | Catalog status |
| `frontend/src/composables/__tests__/useNaturalSort.test.ts` | Frontend unit | Natural sort key generation and comparison. | naturalSortKey splits text+number segments; compareNatural handles numeric ordering. | Changing natural sort logic. | Sorting |
| `frontend/src/components/__tests__/EmptyState.test.ts` | Frontend unit | EmptyState component rendering for all type variants. | All types render correct title/description; custom title overrides; action button emits action event; compact class applied; loading icon spins. | Changing EmptyState component. | UI components |
| `frontend/src/components/__tests__/IndexProgressBar.test.ts` | Frontend unit | IndexProgressBar percent rendering. | Progress bar fill width matches percent prop at 0/50/100. | Changing IndexProgressBar component. | UI components |
| `frontend/src/query/__tests__/keys.test.ts` | Frontend unit | Query key factory functions. | All queryKeys functions return correct key tuples; normalizeQueryPath handles trims/slashes/roots/null. | Changing query key structure. | Query keys |
| `frontend/src/router/__tests__/index.test.ts` | Frontend unit | Router lazy-load prefetch helpers. | prefetchMetadataRoute/LibrariesRoute return Promise and memoize. | Changing router routes. | Router |
| `frontend/src/test/factories.ts` | Frontend test helper | Typed fixture factories for Library, FileNode, browse/status/inspector/facets responses. | All required fields populated with sensible defaults; overrides accepted per-call. | Adding or updating fixture shapes. | Test infrastructure |
| `frontend/src/test/mockApi.ts` | Frontend test helper | Reusable vi.hoisted axios mock factory. | mockApi exposes get/post/patch/delete + interceptors for use with vi.mock('axios'). | Writing new API service tests. | Test infrastructure |
| `frontend/src/test/queryClient.ts` | Frontend test helper | Isolated QueryClient with retry:false + invalidation assertion helper. | retry:false; gcTime:0; expectQueryInvalidated asserts invalidation state. | Writing Vue Query composable tests. | Test infrastructure |
| `frontend/src/test/renderWithApp.ts` | Frontend test helper | Mount components with Pinia + Router + Vue Query in one call. | Fresh Pinia/router/QueryClient per call; createMemoryHistory; common stubs provided. | Mounting Vue components for integration tests. | Test infrastructure |
| `frontend/src/test/__tests__/renderWithApp.test.ts` | Frontend unit | renderWithApp and renderWithAppAsync contract tests. | Sync helper mounts with correct props/router; async helper resolves initialRoute before mount. | Changing test infrastructure. | Test infrastructure |
