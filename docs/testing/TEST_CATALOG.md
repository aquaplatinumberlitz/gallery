# Test Catalog

Catalog entries summarize the regression or contract each important test file protects. File-level headers in the tests provide the same purpose fields close to the code.

| File | Type | Purpose | Guarantees | Run when | Related feature |
| --- | --- | --- | --- | --- | --- |
| `frontend/tests/library-inspector.spec.ts` | Frontend contract | Exercises Library Inspector data-table layout, scoped rows, metadata details, route search behavior, and stale states. | Inspector UI reflects API responses, keeps the metadata search distinct from global gallery search, and hides debug/internal fields by default. | Changing inspector UI, hooks, metadata detail, scope, table layout, or stale notices. | Library Inspector |
| `backend/tests/test_library_inspector.py` | Backend integration | Validates inspector backend scope, search, detail, exclusion, and fallback contracts. | Inspector uses DB/search semantics without altering normal search behavior. | Changing inspector endpoints or app-build exclusion policy. | Library Inspector backend |
| `frontend/tests/metadata-performance.spec.ts` | Frontend perf diagnostic/gate | Measures `/metadata` navigation, backend API timing, render/update timing, search debounce, and state restore. | Timing JSON is emitted; optional env budgets can fail navigation, sort, search, API, request-count, and rendered-row regressions. | Diagnosing metadata slowness, validating inspector perf, or changing metadata route/table/search behavior. | Library Inspector performance |
| `frontend/tests/index-rebuild-flow.spec.ts` | Frontend diagnostic/contract | Covers rebuild flow timing, stale notice behavior, query invalidation, and debug logs. | Rebuild refreshes inspector/status data and emits useful debug-index-rebuild traces. | Changing rebuild, query keys, invalidation, or stale-row UX. | Rebuild/index convergence |
| `frontend/tests/index-status-panel.spec.ts` | Frontend contract | Covers index status copy, scoped/global status separation, popover details, and rebuild/rescan actions. | The primary status dot follows the current scope; global indexer work appears only as secondary info. | Changing `IndexStatusPanel.vue`, index status copy, or `/api/index/status` fields. | Index Status |
| `backend/tests/test_api_integration_index_status.py` | Backend integration | Validates `/api/index/status` response shape, scoped status, global runtime fields, and error serialization. | Scoped status stays serializable and global runtime counters do not overwrite scoped counters. | Changing index status counters, scoped/global response shape, or failed job reporting. | Index Status API |
| `backend/tests/test_indexer_staging.py` | Backend unit/integration | Tests metadata indexer staging, dedupe, flush, retry, scan-yield paths, and scoped runtime counters. | Scan-time enqueueing does not synchronously write SQLite queue state, and runtime activity can be attributed to the correct scope. | Changing indexer queues, staging, retry, scoped runtime accounting, or batch processing. | Metadata indexer |
| `backend/tests/test_warm_folder_listing.py` | Backend integration | Covers SQLite-backed warm folder listings and fallback paths. | Warm listings avoid `scandir` hot-path work and match direct scan behavior. | Changing warm listing, folder index state, natural sort, or pagination. | Warm listing |
| `backend/tests/test_scan_hot_path.py` | Backend contract | Ensures `/api/scan` avoids metadata extraction and PIL opens on hot path. | Cached dimensions and scan counters behave safely. | Changing scan hot path or metadata enqueueing from scan. | Scan performance |
| `backend/tests/test_api_integration_scan.py` | Backend integration | Validates scan response shape, filtering, sorting, pagination, and hot-path contracts. | API scan fields, ignore policy, and natural ordering stay stable. | Changing scan response shape, ignore policy, pagination, or natural sort. | Scan API |
| `backend/tests/test_scan_folder_counts.py` | Backend unit | Covers refresh folder count helpers and warm listing count avoidance. | Folder/image/child counts are accurate for refresh state. | Changing `_scan_folder_counts` or refresh state completeness. | Refresh/warm listing |
| `backend/tests/test_scheduled_refresh.py` | Backend integration | Covers scheduled refresh config, work limits, and safe fallbacks. | Refresh is disabled by default and handles busy/error states safely. | Changing refresh loop, roots, throttling, or folder state updates. | Scheduled refresh |
| `backend/tests/test_watcher.py` | Backend unit | Covers watcher config, debounce, and metadata staging hooks. | Watcher remains disabled by default and handles missing dependency safely. | Changing watcher debounce or image event handling. | File watcher |
| `frontend/tests/lightbox-loading-policy.spec.ts` | Frontend contract | Verifies thumbnail/preview/original endpoint policy. | Normal lightbox open avoids original image fetches; zoom/fullscreen/fallback escalate correctly. | Changing PhotoSwipe source selection or lightbox endpoint policy. | Lightbox source policy |
| `frontend/tests/lightbox-visual-layer.spec.ts` | Frontend visual contract | Checks PhotoSwipe roots, visible images, reopen lifecycle, and EXIF portrait rendering. | No duplicate visible PhotoSwipe images or broken rotated dimensions. | Changing lightbox layers, CSS, or EXIF dimension handling. | PhotoSwipe/lightbox |
| `frontend/tests/mobile-lightbox-sheet.spec.ts` | Frontend responsive contract | Covers mobile gallery controls, lightbox metadata sheet, and mobile search. | Mobile metadata sheet opens/closes repeatedly without trapping the UI. | Changing mobile lightbox, mobile headers, or mobile search. | Mobile lightbox |
| `backend/tests/test_derivatives.py` | Backend unit/integration | Covers derivative sizing, cache versioning, EXIF dimensions, and failures. | Thumbnail/preview aspect ratio, no-upscale, and EXIF orientation stay stable. | Changing derivative generation or dimension caching. | Derivatives |
| `backend/tests/test_api_integration_derivatives.py` | Backend integration | Covers image, thumbnail, and preview API endpoints. | Endpoint cache headers, sizing, safety, and cache separation stay stable. | Changing image serving or derivative cache keys. | Image APIs |
| `frontend/tests/gallery-no-reload.spec.ts` | Frontend contract | Covers no-reload SPA album and lightbox navigation. | Boot identity stays stable and scan requests are not duplicated. | Changing router, gallery navigation, lightbox navigation, or boot init. | SPA navigation |
| `frontend/tests/gallery-no-reload-real-backend.spec.ts` | Frontend real-backend smoke | Repeats no-reload checks with real backend/test images. | Real responses do not cause unexpected full page reloads. | Investigating reloads that do not reproduce with stubs. | Real-backend navigation |
| `frontend/tests/gallery-cache-revisit.spec.ts` | Frontend cache contract | Covers soft revisits and browser-back cache reuse. | Album data survives revisit without duplicate cursor=0 scans. | Changing query keys, cache lifetime, or search clear/back flows. | Query/cache |
| `frontend/tests/search-fielded-ui.spec.ts` | Frontend contract | Covers main gallery plain and fielded search. | Queries, results, clearing, no-results, and special chars stay stable. | Changing search inputs or query serialization. | Search UI |
| `frontend/tests/advanced-search-drawer.spec.ts` | Frontend contract | Covers advanced drawer field controls and result rendering. | Drawer controls send expected search query strings. | Changing advanced search drawer or fielded search request mapping. | Advanced search |
| `backend/tests/test_fielded_search_parser.py` | Backend unit | Covers fielded query parsing and SQL builder behavior. | Field aliases, comparisons, quoted values, and residual terms remain stable. | Changing parser syntax or field aliases. | Fielded search |
| `backend/tests/test_api_integration_metadata_search_facets.py` | Backend integration | Covers metadata, search, fielded queries, and facets endpoints. | Indexed-image response shapes and filters stay stable. | Changing metadata parsing, search SQL, or facets aggregation. | Metadata/search/facets |
| `backend/tests/test_facets.py` | Backend unit/integration | Covers DB-backed facet aggregation. | Facets do not scan filesystem and respect scope/output limits. | Changing facets SQL or LoRA facet handling. | Facets |
| `backend/tests/test_app.py` | Backend broad regression | Covers routes, health, unsafe paths, and broad fielded search semantics. | Route registration and fielded/scoped/CJK search semantics remain intact. | Changing app routes or search integration. | App/search |
| `backend/tests/test_metadata_binary_sanitizer.py` | Backend unit | Covers binary metadata sanitization. | Binary PIL fields do not break JSON storage. | Changing metadata extraction or JSON sanitizer. | Metadata parsing |
| `backend/tests/test_api_integration_health_and_safety.py` | Backend integration | Covers health response and public API path safety. | Unsafe paths are rejected across scan/image/metadata endpoints. | Changing path safety or health response fields. | Health/safety |
| `frontend/tests/responsive-breakpoints.spec.ts` | Frontend responsive contract | Covers mobile, tablet, desktop, large desktop, and transitions. | Layout shells switch cleanly at supported viewport widths. | Changing breakpoints, layout components, or device detection. | Responsive layout |
| `frontend/tests/sidebar-trigger.spec.ts` | Frontend responsive contract | Covers sidebar trigger visibility and behavior. | Sidebar controls stay discoverable without unexpected duplicates. | Changing sidebar trigger, app shell, or sidebar state persistence. | Sidebar |
| `frontend/tests/breadcrumb.spec.ts` | Frontend contract | Covers deep breadcrumb rendering and navigation. | Long paths and breadcrumb clicks preserve SPA navigation behavior. | Changing breadcrumb, route path handling, or header space. | Breadcrumb |
| `frontend/tests/settings-modal.spec.ts` | Frontend contract | Covers settings persistence and landing-page choices. | Intro, theme, and lightbox original-loading preferences persist correctly. | Changing settings modal or localStorage preference keys. | Settings |
| `frontend/tests/fault-injection.spec.ts` | Frontend resilience contract | Covers controlled UI fallback for failed endpoints/offline states. | API failures do not crash gallery, metadata, thumbnails, previews, or lightbox. | Changing API error handling or fallback UI. | Fault tolerance |
| `frontend/tests/tailwind-phase0.spec.ts` | Frontend styling regression | Covers Tailwind phase 0 visual contracts. | Migrated utility styles keep desktop/mobile/tablet/theme baselines stable. | Changing Tailwind setup or token bridge CSS. | Tailwind migration |
| `frontend/tests/tailwind-preflight.spec.ts` | Frontend styling regression | Covers Tailwind preflight absence/regression behavior. | Base component styling is not erased by preflight/global CSS changes. | Changing global CSS order, preflight, or token bridge. | Styling |
| `frontend/tests/perf/album-open.perf.spec.ts` | Frontend perf smoke | Measures album open scan and thumbnail timing. | Perf JSON captures post-click scan/thumbnail timing against budgets. | Validating real-data album open performance. | Performance |
| `frontend/tests/perf/lightbox.perf.spec.ts` | Frontend perf smoke | Measures lightbox open and next-image transition timing. | Preview/original endpoint timing is visible against lightbox budgets. | Validating real-data lightbox performance. | Performance |
| `scripts/perf_scan.py` | Backend perf smoke | Measures repeated `/api/scan` latency against a running backend. | JSON includes min/p50/p95/max; process fails when scan p95 or minimum image expectations regress. | Changing scan, warm listing, cached dimensions, or scan budgets. | Scan performance |
| `scripts/perf_library_inspector.py` | Backend perf smoke | Measures repeated `/api/library/inspector` latency against a running backend. | JSON includes min/p50/p95/max and row counts; process fails when inspector p95 or minimum row expectations regress. | Changing inspector SQL, metadata list DTOs, search filters, or inspector budgets. | Library Inspector performance |
| `scripts/perf_warm_listing.py` | Backend local perf smoke | Compares cold direct scan and warm SQLite listing for a large folder. | Warm listing duration is reported and fails when it exceeds budget. | Changing warm listing SQL, folder index state, or scan fallback behavior. | Warm listing performance |
| `scripts/create_perf_fixture.py` | Perf fixture helper | Creates a deterministic gallery tree and metadata DB for repeatable perf smoke tests. | Generated valid PNGs and seeded metadata rows support scan, thumbnails, lightbox, and inspector perf tests. | Preparing local/CI perf data or changing perf runner defaults. | Performance fixtures |
| `scripts/summarize_perf_reports.py` | Perf reporting helper | Aggregates generated perf report JSON files into one summary. | Writes `perf-summary.json` and `perf-summary.md` with per-report budget status. | After perf smoke tests or when changing perf report shapes. | Performance reporting |
| `scripts/audit_test_matrix.py` | Test audit helper | Collects pytest and Playwright test ids and compares them with the documented test matrix. | Markdown/JSON reports identify catalog drift, matrix gaps, backend coverage gaps, frontend coverage artifact status, and perf report availability. | Auditing test coverage before release, after adding/removing important tests, or when planning missing-test work. | Test gap audit |
| `scripts/test_perf_smoke.sh` | Perf runner | Runs backend scan p95, backend inspector p95, warm listing, album-open, and lightbox perf tests. | Gated perf checks can run against real data or the deterministic fixture. | Before release or after changing performance-sensitive paths. | Performance |
