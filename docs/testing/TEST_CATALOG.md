# Test Catalog

Catalog entries summarize the regression or contract each important test file protects. File-level headers in the tests provide the same purpose fields close to the code.

| File | Type | Purpose | Guarantees | Run when | Related feature |
| --- | --- | --- | --- | --- | --- |
| `frontend/tests/e2e/library-inspector.spec.ts` | Frontend contract | Exercises Library Inspector data-table layout, scoped rows, metadata details, route search behavior, and stale states. | Inspector UI reflects API responses, keeps the metadata search distinct from global gallery search, and hides debug/internal fields by default. | Changing inspector UI, hooks, metadata detail, scope, table layout, or stale notices. | Library Inspector |
| `backend/tests/test_library_inspector.py` | Backend integration | Validates inspector backend scope, search, detail, exclusion, and fallback contracts. | Inspector uses DB/search semantics without altering normal search behavior. | Changing inspector endpoints or app-build exclusion policy. | Library Inspector backend |
| `frontend/tests/e2e/metadata-performance.spec.ts` | Frontend perf diagnostic/gate | Measures `/metadata` navigation, backend API timing, render/update timing, search debounce, and state restore. | Timing JSON is emitted; optional env budgets can fail navigation, sort, search, API, request-count, and rendered-row regressions. | Diagnosing metadata slowness, validating inspector perf, or changing metadata route/table/search behavior. | Library Inspector performance |
| `frontend/tests/e2e/index-rebuild-flow.spec.ts` | Frontend diagnostic/contract | Covers rebuild flow timing, stale notice behavior, query invalidation, and debug logs. | Rebuild refreshes inspector/status data and emits useful debug-index-rebuild traces. | Changing rebuild, query keys, invalidation, or stale-row UX. | Rebuild/index convergence |
| `frontend/tests/e2e/index-status-panel.spec.ts` | Frontend contract | Covers index status copy, scoped/global status separation, popover details, and rebuild/rescan actions. | The primary status dot follows the current scope; global indexer work appears only as secondary info. | Changing `IndexStatusPanel.vue`, index status copy, or `/api/index/status` fields. | Index Status |
| `backend/tests/test_api_integration_index_status.py` | Backend integration | Validates `/api/index/status` response shape, scoped status, global runtime fields, and error serialization. | Scoped status stays serializable and global runtime counters do not overwrite scoped counters. | Changing index status counters, scoped/global response shape, or failed job reporting. | Index Status API |
| `backend/tests/test_indexer_staging.py` | Backend unit/integration | Tests metadata indexer staging, dedupe, flush, retry, scan-yield paths, and scoped runtime counters. | Scan-time enqueueing does not synchronously write SQLite queue state, and runtime activity can be attributed to the correct scope. | Changing indexer queues, staging, retry, scoped runtime accounting, or batch processing. | Metadata indexer |
| `backend/tests/test_warm_folder_listing.py` | Backend integration | Covers SQLite-backed warm folder listings and fallback paths. | Warm listings avoid `scandir` hot-path work and match direct scan behavior. | Changing warm listing, folder index state, natural sort, or pagination. | Warm listing |
| `backend/tests/test_scan_hot_path.py` | Backend contract | Ensures `/api/scan` avoids metadata extraction and PIL opens on hot path. | Cached dimensions and scan counters behave safely. | Changing scan hot path or metadata enqueueing from scan. | Scan performance |
| `backend/tests/test_api_integration_scan.py` | Backend integration | Validates scan response shape, filtering, sorting, pagination, and hot-path contracts. | API scan fields, ignore policy, and natural ordering stay stable. | Changing scan response shape, ignore policy, pagination, or natural sort. | Scan API |
| `backend/tests/test_scan_folder_counts.py` | Backend unit | Covers refresh folder count helpers and warm listing count avoidance. | Folder/image/child counts are accurate for refresh state. | Changing `_scan_folder_counts` or refresh state completeness. | Refresh/warm listing |
| `backend/tests/test_scheduled_refresh.py` | Backend integration | Covers scheduled refresh config, work limits, and safe fallbacks. | Refresh is disabled by default and handles busy/error states safely. | Changing refresh loop, roots, throttling, or folder state updates. | Scheduled refresh |
| `backend/tests/test_watcher.py` | Backend unit | Covers watcher config, debounce, and metadata staging hooks. | Watcher remains disabled by default and handles missing dependency safely. | Changing watcher debounce or image event handling. | File watcher |
| `frontend/tests/e2e/lightbox-loading-policy.spec.ts` | Frontend contract | Verifies thumbnail/preview/original endpoint policy. | Normal lightbox open avoids original image fetches; zoom/fullscreen/fallback escalate correctly. | Changing PhotoSwipe source selection or lightbox endpoint policy. | Lightbox source policy |
| `frontend/tests/e2e/lightbox-visual-layer.spec.ts` | Frontend visual contract | Checks PhotoSwipe roots, visible images, reopen lifecycle, and EXIF portrait rendering. | No duplicate visible PhotoSwipe images or broken rotated dimensions. | Changing lightbox layers, CSS, or EXIF dimension handling. | PhotoSwipe/lightbox |
| `frontend/tests/e2e/mobile-lightbox-sheet.spec.ts` | Frontend responsive contract | Covers mobile gallery controls, lightbox metadata sheet, and mobile search. | Mobile metadata sheet opens/closes repeatedly without trapping the UI. | Changing mobile lightbox, mobile headers, or mobile search. | Mobile lightbox |
| `backend/tests/test_derivatives.py` | Backend unit/integration | Covers derivative sizing, cache versioning, EXIF dimensions, and failures. | Thumbnail/preview aspect ratio, no-upscale, and EXIF orientation stay stable. | Changing derivative generation or dimension caching. | Derivatives |
| `backend/tests/test_api_integration_derivatives.py` | Backend integration | Covers image, thumbnail, and preview API endpoints. | Endpoint cache headers, sizing, safety, and cache separation stay stable. | Changing image serving or derivative cache keys. | Image APIs |
| `frontend/tests/e2e/gallery-no-reload.spec.ts` | Frontend contract | Covers no-reload SPA album and lightbox navigation. | Boot identity stays stable and scan requests are not duplicated. | Changing router, gallery navigation, lightbox navigation, or boot init. | SPA navigation |
| `frontend/tests/e2e/gallery-no-reload-real-backend.spec.ts` | Frontend real-backend smoke | Repeats no-reload checks with real backend/test images. | Real responses do not cause unexpected full page reloads. | Investigating reloads that do not reproduce with stubs. | Real-backend navigation |
| `frontend/tests/e2e/gallery-cache-revisit.spec.ts` | Frontend cache contract | Covers soft revisits and browser-back cache reuse. | Album data survives revisit without duplicate cursor=0 scans. | Changing query keys, cache lifetime, or search clear/back flows. | Query/cache |
| `frontend/tests/e2e/search-fielded-ui.spec.ts` | Frontend contract | Covers main gallery plain and fielded search. | Queries, results, clearing, no-results, and special chars stay stable. | Changing search inputs or query serialization. | Search UI |
| `frontend/tests/e2e/advanced-search-drawer.spec.ts` | Frontend contract | Covers advanced drawer field controls and result rendering. | Drawer controls send expected search query strings. | Changing advanced search drawer or fielded search request mapping. | Advanced search |
| `backend/tests/test_fielded_search_parser.py` | Backend unit | Covers fielded query parsing and SQL builder behavior. | Field aliases, comparisons, quoted values, and residual terms remain stable. | Changing parser syntax or field aliases. | Fielded search |
| `backend/tests/test_api_integration_metadata_search_facets.py` | Backend integration | Covers metadata, search, fielded queries, and facets endpoints. | Indexed-image response shapes and filters stay stable. | Changing metadata parsing, search SQL, or facets aggregation. | Metadata/search/facets |
| `backend/tests/test_facets.py` | Backend unit/integration | Covers DB-backed facet aggregation. | Facets do not scan filesystem and respect scope/output limits. | Changing facets SQL or LoRA facet handling. | Facets |
| `backend/tests/test_app.py` | Backend broad regression | Covers routes, health, unsafe paths, and broad fielded search semantics. | Route registration and fielded/scoped/CJK search semantics remain intact. | Changing app routes or search integration. | App/search |
| `backend/tests/test_metadata_binary_sanitizer.py` | Backend unit | Covers binary metadata sanitization. | Binary PIL fields do not break JSON storage. | Changing metadata extraction or JSON sanitizer. | Metadata parsing |
| `backend/tests/test_api_integration_health_and_safety.py` | Backend integration | Covers health response and public API path safety. | Unsafe paths are rejected across scan/image/metadata endpoints. | Changing path safety or health response fields. | Health/safety |
| `frontend/tests/e2e/responsive-breakpoints.spec.ts` | Frontend responsive contract | Covers mobile, tablet, desktop, large desktop, and transitions. | Layout shells switch cleanly at supported viewport widths. | Changing breakpoints, layout components, or device detection. | Responsive layout |
| `frontend/tests/e2e/sidebar-trigger.spec.ts` | Frontend responsive contract | Covers sidebar trigger visibility and behavior. | Sidebar controls stay discoverable without unexpected duplicates. | Changing sidebar trigger, app shell, or sidebar state persistence. | Sidebar |
| `frontend/tests/e2e/breadcrumb.spec.ts` | Frontend contract | Covers deep breadcrumb rendering and navigation. | Long paths and breadcrumb clicks preserve SPA navigation behavior. | Changing breadcrumb, route path handling, or header space. | Breadcrumb |
| `frontend/tests/e2e/settings-modal.spec.ts` | Frontend contract | Covers settings persistence and landing-page choices. | Intro, theme, and lightbox original-loading preferences persist correctly. | Changing settings modal or localStorage preference keys. | Settings |
| `frontend/tests/e2e/fault-injection.spec.ts` | Frontend resilience contract | Covers controlled UI fallback for failed endpoints/offline states. | API failures do not crash gallery, metadata, thumbnails, previews, or lightbox. | Changing API error handling or fallback UI. | Fault tolerance |
| `frontend/tests/e2e/tailwind-phase0.spec.ts` | Frontend styling regression | Covers Tailwind phase 0 visual contracts. | Migrated utility styles keep desktop/mobile/tablet/theme baselines stable. | Changing Tailwind setup or token bridge CSS. | Tailwind migration |
| `frontend/tests/e2e/tailwind-preflight.spec.ts` | Frontend styling regression | Covers Tailwind preflight absence/regression behavior. | Base component styling is not erased by preflight/global CSS changes. | Changing global CSS order, preflight, or token bridge. | Styling |
| `frontend/tests/e2e/perf/album-open.perf.spec.ts` | Frontend perf smoke | Measures album open scan and thumbnail timing. | Perf JSON captures post-click scan/thumbnail timing against budgets. | Validating real-data album open performance. | Performance |
| `frontend/tests/e2e/perf/lightbox.perf.spec.ts` | Frontend perf smoke | Measures lightbox open and next-image transition timing. | Preview/original endpoint timing is visible against lightbox budgets. | Validating real-data lightbox performance. | Performance |
| `backend/tests/test_metadata_extract_parsers.py` | Backend unit | Tests A1111, ComfyUI, SwarmUI, NovelAI, EasyDiffusion metadata parsers and dispatch logic. | Each parser handles valid/invalid/malformed input without crashing; dispatch selects the correct parser by metadata source. | Changing metadata extraction parsers, adding new parser, or dispatch logic. | Metadata extraction |
| `backend/tests/test_folders.py` | Backend integration | Covers folder listing children (hidden, excluded, error branches) and API routes (safe paths, unsafe paths, open-folder). | Folder listing handles scandir OSError, PermissionError, and resolve errors; API routes reject unsafe paths gracefully. | Changing folder route behavior, ignore/exclude logic, or open-folder feature. | Folder management |
| `backend/tests/test_static_files.py` | Backend integration | Covers static file serving: read_root (dev vs prod), landing pages listing, and catch-all routing. | Dev mode returns API marker JSON; production serves index.html for SPA routes and falls back to index.html for unknown assets. | Changing static file serving, landing pages, or production/dev mode behavior. | Static file serving |
| `backend/tests/test_refresh_service.py` | Backend unit | Covers _refresh_folder (missing path, success, error), _run_refresh_tick (roots, allow_all, max_folders, stop event), and lifecycle (start, stop, status). | Refresh tick respects roots, max folders, and stop event; refresh lifecycle is idempotent and thread-safe. | Changing scheduled refresh logic, root filtering, or lifecycle management. | Scheduled refresh |
| `backend/tests/test_albums.py` | Backend unit | Covers has_subfolders, first_images_in_dir, count_images_in_dir, build_album_metadata with hidden dirs, scandir OSError, stat failures, and resolve errors. | Album helpers handle missing paths, stat errors, and scandir OSError safely; hidden dirs are skipped. | Changing album listing or metadata building logic. | Album management |
| `backend/tests/test_paths.py` | Backend unit | Covers resolve_path (OSError re-raise, Windows fallback) and is_path_safe (resolve OSError, root boundary checks). | Path resolution and safety checks handle OSError and boundary violations without crashing. | Changing path resolution or security boundary checks. | Path security |
| `backend/tests/test_metadata_store_coverage.py` | Backend coverage | Covers edge cases in metadata_store: mark_folder_index_incomplete, update_folder_index_state, upsert_image_dimensions, upsert_metadata_result, queue_metadata_index_paths, cleanup_stale_index, cleanup_ignored_index, index_directory_tree, and initialize_database idempotency. | All metadata_store functions handle missing files, non-image paths, OSError, and SQLite errors safely. | Adding or changing metadata store internals, image dimension caching, or index directory tree traversal. | Metadata store coverage |
| `backend/tests/test_thumbnails_coverage.py` | Backend coverage | Covers error paths in thumbnails: normalize_derivative_format, render_derivative_impl (palette, RGBA, JPEG), generate_derivative (cache, decompression bomb, unidentified image), resolve_image_request_path, serve_derivative (error mapping, 304, fallback), and API endpoint error handling. | Derivative generation handles format conversion, alpha flattening, and cache hits; serve endpoints map all errors to correct HTTP status codes. | Changing thumbnail generation, derivative format handling, error mapping, or API response behavior. | Thumbnails coverage |
| `backend/tests/test_search_coverage.py` | Backend coverage | Covers search edge cases: empty query returns empty, fielded search zero results, metadata detail endpoints (missing path, server error), and library inspector overscan (invalid cursor, failure, truncated results). | Search endpoints handle empty queries, missing paths, overscan failures, and truncated results with correct HTTP status codes. | Changing search endpoints, library inspector overscan logic, or metadata detail behavior. | Search coverage |
| `backend/tests/test_app_coverage.py` | Backend coverage | Covers app startup: CORS origins (FRONTEND_ORIGIN, FRONTEND_PORT, defaults), startup hook calling refresh and watcher, and ENABLE_PROFILER/ENABLE_METRICS branches. | CORS origin logic handles all config variants; startup hook correctly initializes refresh and watcher; profiler/metrics branches are gated by env vars. | Changing CORS configuration, app startup hooks, or profiler/metrics initialization. | App infrastructure coverage |
| `backend/tests/test_metadata_parse_coverage.py` | Backend coverage | Covers metadata parse error paths: missing file (404), invalid image (400), uncached generic exception (400), cache hit bypass, stat failure (404), producer error wrapping (OSError→404, APIError→reraise, generic→500), and _estimate_dict_size edge cases (TypeError, circular ref). | Metadata parse handles all error conditions with correct HTTP status codes; inflight deduplication prevents duplicate parse work. | Changing metadata parsing error handling, cache behavior, or inflight deduplication. | Metadata parsing coverage |
| `scripts/perf_scan.py` | Backend perf smoke | Measures repeated `/api/scan` latency against a running backend. | JSON includes min/p50/p95/max; process fails when scan p95 or minimum image expectations regress. | Changing scan, warm listing, cached dimensions, or scan budgets. | Scan performance |
| `scripts/perf_library_inspector.py` | Backend perf smoke | Measures repeated `/api/library/inspector` latency against a running backend. | JSON includes min/p50/p95/max and row counts; process fails when inspector p95 or minimum row expectations regress. | Changing inspector SQL, metadata list DTOs, search filters, or inspector budgets. | Library Inspector performance |
| `scripts/perf_warm_listing.py` | Backend local perf smoke | Compares cold direct scan and warm SQLite listing for a large folder. | Warm listing duration is reported and fails when it exceeds budget. | Changing warm listing SQL, folder index state, or scan fallback behavior. | Warm listing performance |
| `scripts/create_perf_fixture.py` | Perf fixture helper | Creates a deterministic gallery tree and metadata DB for repeatable perf smoke tests. | Generated valid PNGs and seeded metadata rows support scan, thumbnails, lightbox, and inspector perf tests. | Preparing local/CI perf data or changing perf runner defaults. | Performance fixtures |
| `scripts/summarize_perf_reports.py` | Perf reporting helper | Aggregates generated perf report JSON files into one summary. | Writes `perf-summary.json` and `perf-summary.md` with per-report budget status. | After perf smoke tests or when changing perf report shapes. | Performance reporting |
| `scripts/audit_test_matrix.py` | Test audit helper | Collects pytest, Playwright, and vitest test ids and compares them with the documented test matrix. | Markdown/JSON reports identify catalog drift, matrix gaps, backend coverage gaps, frontend coverage artifact status, and perf report availability. | Auditing test coverage before release, after adding/removing important tests, or when planning missing-test work. | Test gap audit |
| `scripts/test_perf_smoke.sh` | Perf runner | Runs backend scan p95, backend inspector p95, warm listing, album-open, and lightbox perf tests. | Gated perf checks can run against real data or the deterministic fixture. | Before release or after changing performance-sensitive paths. | Performance |
| `frontend/src/utils/__tests__/fuzzySearch.test.ts` | Frontend unit (vitest) | Tests Fuse-backed fuzzy matching against FileNode lists, covering empty/whitespace queries, `includePath` branching, cache reuse, special characters, and CJK input. | Empty/whitespace queries return the full list; `includePath=true` matches path tokens; cache is reused for repeated calls on the same array; special chars and CJK do not crash. | After modifying `src/utils/fuzzySearch.ts`, Fuse options, or FileNode matching weights. | Fuzzy search |
| `frontend/src/utils/__tests__/indexStatus.test.ts` | Frontend unit (vitest) | Tests scoped and global index status counts, failed/active/queued/known-updates predicates, UI status mapping, refetch interval, and progress info derivation from `IndexStatusResponse`. | All predicates reflect scoped vs global runtime counters; UI status precedence (error > unknown > warning > indexing > stale > ready) is stable; refetch interval tightens to 2.5s during active work. | After modifying `src/utils/indexStatus.ts`, scoped/global status shape, or refetch interval logic. | Index Status |
| `frontend/src/utils/__tests__/indexStatusCopy.test.ts` | Frontend unit (vitest) | Tests field copy/label/tooltip resolution and debug-mode apiTrace appending for the Index Status panel. | Cataloged fields return their copy; unknown fields fall back to the raw name with empty tooltip; debug mode appends `apiTrace` only for fields with non-empty tooltip. | After changing `INDEX_FIELD_COPY`, `INDEX_STATUS_LABELS`, or debug apiTrace behavior. | Index Status copy |
| `frontend/src/utils/__tests__/indexMaintenance.test.ts` | Frontend unit (vitest) | Tests scope rebuild marker mark/get/clear across nested roots, finite-timestamp guards, and root-path boundary checks. | Markers propagate to descendant paths; `isPathInsideRoot` boundary check uses `${root}/`; non-finite timestamps and empty paths are ignored; `clearScopeRebuildMarker` only removes markers whose root covers the cleared scope. | After changing scope rebuild markers, rebuild invalidation, or path boundary checks. | Index rebuild markers |
| `frontend/src/utils/__tests__/serializeAdvancedSearchToQuery.test.ts` | Frontend unit (vitest) | Tests fielded search serialization (`serializeAdvancedSearchToQuery`, `filterToDisplayString`) and parsing (`parseFieldedQuery`) including literal-field operator omission, value quoting/escaping, and round-trips. | Literal fields (ratio/size/date) omit the operator; values with whitespace/quotes/parens are quoted and escaped; parse round-trips recover the original filters. | After changing fielded search syntax, parser regex, or display string formatting. | Advanced search |
| `frontend/src/utils/__tests__/loraHighlighter.test.ts` | Frontend unit (vitest) | Tests `<lora:...>` tokenization, HTML escaping of plain text and token contents, weight/name handling, and case-insensitive tag matching. | Lora tokens are wrapped in `span.lora-pill`; HTML special chars in plain text and inside name/weight are escaped; malformed tokens fall back to escaped plain text. | After changing lora highlighter regex or escape behavior. | Lora highlighter |
| `frontend/src/utils/__tests__/lightbox.test.ts` | Frontend unit (vitest) | Tests PhotoSwipe item building, dimension validation, animated-asset detection, and the `shouldAlwaysLoadOriginal` localStorage preference. | `buildPhotoSwipeItem` uses provided dimensions or falls back to 1200x1200; `isLikelyAnimatedAsset` handles gif/apng, query strings, and uppercase extensions; localStorage errors are swallowed. | After changing PhotoSwipe item shape, dimension helpers, or animated-asset detection. | Lightbox helpers |
| `frontend/src/stores/__tests__/gallery.test.ts` | Frontend unit (vitest) | Tests gallery store state management: root path lifecycle, sidebar tree normalization, history navigation, sort persistence, folder expansion, and error/toast handling for `setRootPath`/`openInExplorer`. | Store actions produce expected state transitions; `GalleryAPIError` surfaces user-facing suggestions; non-API errors use the fallback message; sort preference persists to localStorage. | After modifying gallery store, adding new store actions, or changing error/toast integration. | Gallery store |
| `frontend/src/stores/__tests__/toast.test.ts` | Frontend unit (vitest) | Tests toast store: add/remove/clear, MAX_TOASTS clamping, per-type durations, auto-remove timers, and convenience helpers (`success`/`error`/`warning`/`info`). | activeToasts limits visible toasts to 3; durations follow the SHORT/DEFAULT/MEDIUM/LONG constants; persistent toasts (duration 0) are not auto-removed. | After modifying toast store, duration constants, or convenience helper defaults. | Toast store |
| `frontend/src/stores/__tests__/lightbox.test.ts` | Frontend unit (vitest) | Tests lightbox store: open/close, image filtering, index resolution (preferredIndex vs path lookup), neighbor preloading, and dimension remembering. | Non-image items are filtered from navigation; preferredIndex is used only when its item path matches the requested path; thumbnail-source dimensions are not remembered. | After modifying lightbox store, navigation logic, or dimension memory. | Lightbox store |
| `frontend/src/composables/__tests__/useToast.test.ts` | Frontend unit (vitest) | Tests the `useToast` composable wrappers and the promise-based toast flow on top of the toast store. | Convenience wrappers delegate to the store with the correct type and duration; `promise()` removes the loading toast and shows success/error based on the promise result. | After modifying `useToast` composable or promise-based toast flow. | Toast composable |
| `frontend/src/composables/__tests__/usePullToRefresh.test.ts` | Frontend unit (vitest) | Tests the pull-to-refresh gesture state machine: axis lock, threshold/max distance, haptic feedback, refresh in-flight guard, and computed indicators. | Horizontal axis lock resets pull state; pullDistance is capped at maxDistance and scaled by 0.5; refresh triggers only at pullDistance >= threshold; haptic medium fires on threshold cross. | After changing pull-to-refresh state machine, threshold defaults, or haptic integration. | Pull-to-refresh |
| `frontend/src/composables/__tests__/useGalleryTheme.test.ts` | Frontend unit (vitest) | Tests theme mode persistence, set/toggle/cycle, view-transition selection, and `prefers-reduced-motion` fallback. | `setTheme`/`toggleTheme`/`cycleTheme` update mode and persist under `gallery-theme`; `document.startViewTransition` is used when available unless prefers-reduced-motion matches; otherwise the `theme-transitioning` class is added. | After changing theme composable, view-transition integration, or reduced-motion handling. | Gallery theme |
| `frontend/src/composables/__tests__/useNaturalSort.test.ts` | Frontend unit (vitest) | Tests natural-sort key generation and the comparator (numeric chunks, case-insensitivity, missing chunks). | Numeric chunks compare by numeric value not string; text chunks are case-insensitive; shorter strings sort before longer strings sharing a prefix. | After changing natural sort key/comparator. | Natural sort |
| `frontend/src/composables/__tests__/useDelayedBoolean.test.ts` | Frontend unit (vitest) | Tests the timer-based boolean with cancellation, rescheduling, and unmount cleanup. | The delayed ref flips true only after the configured delay; flipping the source to false cancels the pending timer; re-flipping true reschedules; unmount clears the timer. | After changing `useDelayedBoolean` timer/cancellation logic. | Delayed boolean |
| `frontend/src/composables/__tests__/useHaptic.test.ts` | Frontend unit (vitest) | Tests the `navigator.vibrate` wrapper: capability detection and light/medium patterns. | `canVibrate` reflects `navigator.vibrate` availability; `light()` calls vibrate(10); `medium()` calls vibrate(20); unavailable vibrate does not throw. | After changing `useHaptic` capability guards or vibration patterns. | Haptic feedback |
| `frontend/src/composables/__tests__/useClipboard.test.ts` | Frontend unit (vitest) | Tests clipboard copy: `navigator.clipboard.writeText` path, `document.execCommand('copy')` fallback, per-id labels, error toasts, and status reset timer. | Clipboard API path flips copyStatus to true; execCommand fallback works when clipboard is unavailable; failed copies show an error toast; copyStatus resets after 1500ms. | After changing clipboard composable, fallback logic, or label mapping. | Clipboard |
| `frontend/src/composables/__tests__/useColumnResize.test.ts` | Frontend unit (vitest) | Tests slider level mapping per device category, localStorage persistence + legacy column-count migration, and row-height recomputation. | `effectiveColumnCount` maps slider level through the responsive column map; legacy raw column counts >5 are migrated to the matching level; level changes persist and recompute rowHeight. | After changing grid column map, slider levels, or legacy migration. | Column resize |
| `frontend/src/composables/__tests__/useFocusTrap.test.ts` | Frontend unit (vitest) | Tests focus trap: focusable element discovery, Tab/Shift+Tab wrap-around, activate/deactivate, and rAF focus scheduling. | Disabled buttons are excluded; Tab on the last element wraps to the first; Shift+Tab on the first wraps to the last; `deactivate()` removes the keydown listener. | After changing focus trap selectors, wrap-around logic, or activate/deactivate lifecycle. | Focus trap |
| `frontend/src/composables/__tests__/useScrollVisibility.test.ts` | Frontend unit (vitest) | Tests scroll-driven bar visibility: down-hide/up-show, bottom guard, polling-vs-container-ref attach paths, and unmount cleanup. | Scrolling down hides bars and scrolling up shows them; the bottom guard prevents toggles within 150px of the bottom; container-ref mode skips polling; unmount clears the interval and observer. | After changing scroll visibility observer, polling cadence, or bottom guard. | Scroll visibility |
