Task: Fix broken mobile sheet layout after integrating @douxcode/vue-spring-bottom-sheet.

Branch: test/vue-spring-bottom-sheet-mobile-lightbox
Repo: /home/ubuntu/gallery-repo
Frontend: /home/ubuntu/gallery-repo/frontend/

Context:
The Vue Spring Bottom Sheet prototype now opens, but layout is broken on real mobile.

Observed issues:
- Gray vertical strip on left side of sheet content
- Prompt / Params / Model content clipped/squeezed
- Params chips render as tiny vertical letters on the left
- Prompt/Negative content shifted/clipped
- Header tabs look mostly okay but body layout broken

Goal: Make content render correctly. Layout integration only, not UX changes.

Scope:
- LightboxMobileSheet.vue
- _lightbox-mobile.scss

Do NOT touch: desktop/tablet lightbox, PhotoSwipe lifecycle, metadata parsing, ExpandableText logic, gallery grid, backend

Requirements:

1. Inspect the actual rendered DOM structure from the library.
Read: frontend/node_modules/@douxcode/vue-spring-bottom-sheet/dist/index.mjs
Look at the template to understand:
- What root element is generated
- What scroll container is used (data-vsbs-scroll)
- How header slot, default slot, footer slot are positioned
- How content-class prop is applied
- Any wrapper divs that may clip or constrain width

2. Look at current _lightbox-mobile.scss for old selectors that assume the old custom sheet DOM:
- .sheet-panel (old custom panel)
- .sheet-content (old content div)
- .sheet-body
- .mobile-sheet
- .prompt-body
- .metadata-panel
- any fixed width, absolute positioning, or overflow rules
The library provides its own structure via data attributes (data-vsbs-sheet, data-vsbs-scroll, data-vsbs-content, data-vsbs-header, data-vsbs-footer).
Old CSS targeting the removed custom DOM will break layout.

3. Fix the template in LightboxMobileSheet.vue:
Current template:
<div class="sheet-content-inner" :class="{ 'sheet-content-enter': true }">
  ... all metadata content ...
</div>

This div is inside the BottomSheet default slot. The library wraps the default slot in:
[data-vsbs-scroll] > [data-vsbs-content-wrapper] > [data-vsbs-content contentClass] > our content

So .sheet-content-inner is already inside several library wrappers. It just needs:
- width: 100%
- min-width: 0
- overflow-x: hidden
- proper padding

Current template already works but the old CSS in _lightbox-mobile.scss for .sheet-panel, .sheet-handle-wrapper, .sheet-handle, .sheet-backdrop etc was for the OLD custom sheet. These selectors are now targeting nothing (dead CSS) OR conflicting because some names overlap with library elements.

Actually, wait - looking more carefully:
- .sheet-panel CSS still exists in _lightbox-mobile.scss with height:44dvh, transform, transition, overflow:hidden
- .sheet-handle-wrapper, .sheet-handle- these are OLD custom sheet CSS
- .sheet-content with overflow-y: auto, padding, etc. - this class is now ALSO used as content-class="sheet-content" on BottomSheet. So the library renders [data-vsbs-content].sheet-content, and our old .sheet-content CSS applies to it.

BUT .sheet-content had:
.sheet-content {
  flex: 1;
  overflow-y: auto;
  overscroll-behavior: contain;
  touch-action: pan-y;
  padding: 12px 16px 24px;
  -webkit-overflow-scrolling: touch;
  &::-webkit-scrollbar { width: 4px; }
}

This should be OK for the library's content area. The issue might be that the library ALSO has its own scroll container [data-vsbs-scroll] that wraps around [data-vsbs-content], and our styles for .sheet-content are overridden or conflicting.

Actually the real issue is that the OLD .sheet-panel CSS has:
.sheet-panel {
  position: relative;
  height: 44dvh;
  max-height: 80dvh;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  ...
}

And .sheet-backdrop, .sheet-handle-wrapper, .sheet-handle are ALL still in the CSS but the DOM elements they target no longer exist (we replaced the custom sheet with BottomSheet). These are dead CSS doing nothing.

But more importantly, there might be CSS that's actively conflicting. Let me think about what could cause a gray strip on the left...

Actually, looking at the _lightbox-mobile.scss file, the :global([data-vsbs-sheet]) section at the top sets --vsbs-backdrop-bg. But the sheet's background is set by --vsbs-background. The issue might be something else.

Let me think about the gray strip on the left. It could be:
1. The library's backdrop showing through somehow
2. Padding/margin on the sheet container
3. The scroll container having left padding
4. Some old .sheet-backdrop or .mobile-sheet CSS still applying to something

Actually, the .mobile-sheet CSS was DELETE in the previous change. Let me check... Yes, in the last Codex fix, .mobile-sheet was replaced with :global([data-vsbs-sheet]).

So the old .sheet-panel, .sheet-handle-wrapper, .sheet-handle, .sheet-backdrop CSS selectors are all dead code now. They don't target any DOM elements because we removed those from the template. They're harmless dead CSS.

The actual problem might be simpler:
1. The library's content scroll container might have restricted width
2. Or the .sheet-content-inner div doesn't have proper width
3. Or the params-grid/flex wrap items need proper width

Let me just focus the fix:
- Remove old dead CSS selectors (.sheet-panel, .sheet-handle-wrapper, .sheet-handle, .sheet-backdrop, .sheet-content-enter animation)
- Ensure .sheet-content-inner has width:100%, min-width:0, overflow-x:hidden
- Ensure .params-grid and .param-pill don't have width restrictions that cause vertical text
- Use :global([data-vsbs-content].sheet-content) for the library content area
- Make sure header slot renders properly within the library's header area

4. Make these changes:

In _lightbox-mobile.scss:
- Remove: .sheet-panel, .sheet-handle-wrapper, .sheet-handle, .sheet-backdrop selectors (they're dead, target old custom DOM)
- Keep .sheet-header, .sheet-tabs, .sheet-tab, .sheet-expand-toggle, .sheet-content, .section-top, .inline-copy-button, .inline-copy-icon, .meta-section, etc.
- Remove: .sheet-content-enter animation (it's on .sheet-content-inner but the library handles animations)
- Ensure :global([data-vsbs-content].sheet-content) has proper padding and width
- Ensure .sheet-content-inner has:
  width: 100%;
  min-width: 0;
  box-sizing: border-box;
  overflow-x: hidden;

In LightboxMobileSheet.vue:
- Keep current template structure
- Ensure .sheet-content-inner div is present and wraps all metadata content
- Keep v-model, teleport-defer, blocking=false

In _lightbox-mobile.scss, look at .params-grid and .param-pill to ensure they don't restrict width:

.params-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  width: 100%;
}

.param-pill {
  display: flex;
  align-items: center;
  gap: 8px;
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.1);
  padding: 4px 8px;
  border-radius: 6px;
  font-size: 12px;
  min-width: 0;
  max-width: 100%;
  overflow: hidden;
  ...
}

5. Verification:
cd /home/ubuntu/gallery-repo/frontend
npm run build

6. Do NOT commit.
