Task: Fix Vue Spring Bottom Sheet mobile metadata sheet width chain and theme background.

Branch: test/vue-spring-bottom-sheet-mobile-lightbox
Repo: /home/ubuntu/gallery-repo
Frontend: /home/ubuntu/gallery-repo/frontend/

Context:
Vue Spring Bottom Sheet opens and renders content but:
1. Width chain collapses: [data-vsbs-scroll] width=4px, [data-vsbs-content] width=24px, .expandable-text width=0px
2. [data-vsbs-scroll] has background: rgba(255,255,255,0.2) causing gray sheet body instead of black/dark theme

Manually patching width via devtools fixes display, confirming pure CSS/layout issue.

Goal:
1. Fix the VSBS content width chain permanently.
2. Override [data-vsbs-scroll] background to match black mobile sheet theme.
3. Do not restore old custom drag code.

Scope:
- frontend/src/styles/_lightbox-mobile.scss
- frontend/src/components/LightboxMobileSheet.vue only if class/prop wiring needed

Do NOT touch: desktop/tablet lightbox, PhotoSwipe, metadata parsing, Show more, tabs, backend, gallery grid

Requirements:

1. Fix width chain.

Ensure these nodes stretch to full width via CSS:

:global([data-vsbs-scroll]) {
  width: 100%;
  min-width: 0;
  inline-size: 100%;
  box-sizing: border-box;
}

:global([data-vsbs-content]) {
  width: 100%;
  min-width: 0;
  inline-size: 100%;
  box-sizing: border-box;
}

:global([data-vsbs-content].sheet-content) {
  width: 100%;
  min-width: 0;
  box-sizing: border-box;
  padding: 12px 16px 24px;
}

.sheet-content-inner {
  width: 100%;
  min-width: 0;
  box-sizing: border-box;
  overflow-x: hidden;
}

.sheet-tab-content {
  width: 100%;
  min-width: 0;
}

.meta-section {
  min-width: 0;
}

:global(.expandable-text) {
  min-width: 0;
  width: 100%;
}

2. Fix gray background.

:global([data-vsbs-scroll]) {
  background: transparent !important;
}

:global([data-vsbs-sheet]) {
  --vsbs-background: var(--gallery-lightbox-bg, #1a1a1a);
  background: var(--gallery-lightbox-bg, #1a1a1a);
  color: #e5e7eb;
}

:global([data-vsbs-content].sheet-content) {
  background: transparent;
}

.sheet-header {
  background: var(--gallery-lightbox-bg, #1a1a1a);
}

Current file: frontend/src/styles/_lightbox-mobile.scss
The old .sheet-content selector (which used flex:1 etc) was already removed. The current CSS targets:
- :global([data-vsbs-sheet]) with CSS variables, z-index
- :global([data-vsbs-backdrop])
- :global([data-vsbs-header].mobile-sheet-header-slot)
- :global([data-vsbs-scroll]) with width:100%, overflow-y:auto, scrollbar styles
- :global([data-vsbs-content].sheet-content) with width:100%, padding, user-select
- .sheet-content-inner with width:100%, overflow-x:hidden
- .sheet-tab-content, .meta-section, .params-grid, .param-pill
- .sheet-header, .sheet-tabs, .sheet-tab, .sheet-expand-toggle
- mobile responsive overrides

The width chain collapse might be because the library renders:
[data-vsbs-sheet] > [shadow] > [data-vsbs-scroll] > [data-vsbs-content-wrapper] > [data-vsbs-content] .sheet-content > .sheet-content-inner > content

If any of these intermediate wrappers (like data-vsbs-content-wrapper) lacks width:100%, the chain collapses.

Check what the library renders. Read:
frontend/node_modules/@douxcode/vue-spring-bottom-sheet/dist/index.mjs
Search for: data-vsbs-content-wrapper, data-vsbs-content, sheetContent, sheetScroll

3. Apply these CSS rules:

In _lightbox-mobile.scss:

a) Ensure :global([data-vsbs-scroll]) has:
   width: 100%;
   min-width: 0;
   inline-size: 100%;
   box-sizing: border-box;
   background: transparent !important;

b) Ensure :global([data-vsbs-content]) has:
   width: 100%;
   min-width: 0;
   box-sizing: border-box;

c) Ensure :global([data-vsbs-content-wrapper]) has:
   width: 100%;
   min-width: 0;
   box-sizing: border-box;
   (add this selector if it exists in the library DOM)

d) Update :global([data-vsbs-content].sheet-content):
   width: 100%;
   min-width: 0;
   box-sizing: border-box;

e) Update :global([data-vsbs-sheet]):
   background matches theme

f) .sheet-content-inner: keep as is
g) .sheet-tab-content: keep as is

4. Verification

cd /home/ubuntu/gallery-repo/frontend
npm run build

5. Do NOT commit.

Report what was fixed.
