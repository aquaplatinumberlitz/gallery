Task: Investigate whether VSBS backdrop can be enabled safely inside PhotoSwipe 5 mobile lightbox.

Branch: main (VSBS already merged)
Repo: /home/ubuntu/gallery-repo
Frontend: /home/ubuntu/gallery-repo/frontend/

Context: Mobile metadata sheet uses @douxcode/vue-spring-bottom-sheet inside PhotoSwipe 5. blocking=false avoids focus recursion but also means no backdrop to close sheet when tapping photo area outside.

Question: Can we enable VSBS backdrop / outside tap close safely without reintroducing PhotoSwipe focus/gesture conflicts?

Do NOT change code yet. Investigation only.

Investigation steps:

1. Read current LightboxMobileSheet.vue to get exact BottomSheet props.
2. Read VSBS library source to understand blocking, backdrop, focus trap relationship.
   File: frontend/node_modules/@douxcode/vue-spring-bottom-sheet/dist/index.mjs
   Look at: the render function around lines 448-460 where backdrop is created.
   Look at: the focus trap setup (st function around line 209).
   Look at: the scroll lock setup (ut function around line 239).
   Understand: does blocking gate ALL of backdrop+focusTrap+scrollLock, or can they be individually controlled?
   Search for: activate, deactivate, blocking.value, forceMount, backdropRef

3. Read the VSBS types file for any missing props.
   File: frontend/node_modules/@douxcode/vue-spring-bottom-sheet/dist/types.d.ts

4. Read our current focus trap usage.
   File: frontend/src/composables/useFocusTrap.ts (if exists) or frontend/src/components/Lightbox.vue

5. Read our current PhotoSwipe options.
   File: frontend/src/components/MobilePhotoSwipe.vue
   File: frontend/src/composables/usePhotoSwipe.ts

Key questions to research:

A. What exactly caused the original focus trap recursion?
   - Was it VSBS blocking=true focus trap?
   - Was it VSBS teleport to body?
   - Was it imperative open() timing?
   - Was it PhotoSwipe focus trap fighting VSBS focus trap?
   Look at Lightbox.vue for useFocusTrap composable usage.

B. Is VSBS backdrop separable from focus trap?
   - Can backdrop exist without blocking?
   - Is there a prop to have backdrop but no focus trap?
   - Does forceMount help?
   - Can we use teleport-to="false" to keep Dom inside component tree?

C. If we wanted to test blocking=true:
   - blocking=true
   - canBackdropClose=true
   - canSwipeClose=true
   - teleport-defer (already set)
   - v-model
   - BE CAREFUL - may cause recursion again
   - If safe to test: temporarily modify LightboxMobileSheet.vue blocking prop to true
   - Run npm run build
   - Then REVERT immediately after testing
   - Report result

D. Can we create our own lightweight backdrop instead?
   - Keep VSBS blocking=false
   - Add a custom div backdrop inside LightboxMobileSheet.vue that sits between PhotoSwipe and VSBS sheet
   - Tap backdrop closes sheet
   - No focus trap
   - No scroll lock

Report format:

## Summary

## Original conflict analysis

## VSBS findings

## PhotoSwipe findings

## Options

### Option A: Keep current non-modal behavior
### Option B: Use VSBS backdrop
### Option C: Custom lightweight backdrop/tap-outside close
### Option D: Explicit close button

## Recommendation
