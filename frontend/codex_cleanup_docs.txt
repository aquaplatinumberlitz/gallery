Task: Final cleanup for updated docs on branch refactor/tanstack-virtual-scroll.

Repo: /home/ubuntu/gallery-repo (currently on main at b515d33)

Context:
- Branch already migrated virtual grid from vue-virtual-scroller to @tanstack/vue-virtual.
- Docs were updated, but a few stale/fragile notes remain.
- This task is docs-only unless a docs check/build requires a tiny non-runtime correction.
- Do NOT change frontend runtime code.
- Do NOT change backend.
- Do NOT change dependencies.
- Do NOT refactor components.

Before editing:
1. Pull latest:
   git checkout refactor/tanstack-virtual-scroll
   git pull origin refactor/tanstack-virtual-scroll

2. Read current docs first:
   - README.md
   - docs/ARCHITECTURE.md
   - docs/TROUBLESHOOTING.md
   - docs/THIRD_PARTY_LIBRARIES.md
   - docs/DEVELOPMENT.md
   - docs/UI_UX_GUIDELINES.md

3. Search docs and frontend for stale references:
   - vue-virtual-scroller
   - RecycleScroller
   - DynamicScroller
   - .vue-recycle-scroller
   - @tanstack/vue-virtual
   - TanStack Query
   - Pinia
   - Last reviewed
   - npm audit

Fix these docs issues:

1. README date
   Update Last reviewed from "2026-06-06" to "2026-06-08" in README.md.

2. Troubleshooting stale virtual-scroller selector
   In docs/TROUBLESHOOTING.md, update the Scroll Visibility section.
   Remove stale .vue-recycle-scroller from the fallback selector explanation.
   Replace it with wording like:
     - useScrollVisibility.ts should prefer an injected scroll container ref when available.
     - Fallback DOM selectors should match current containers such as .tanstack-scroller, .mobile-scroller, .scroller, and .folders-only-container.
     - Selector fallback is a backup only; avoid relying on generic .scroller when a specific ref/class is available.
   Keep the warning that changing scroll container class names can break mobile bar visibility.

3. Architecture dependency audit note
   In docs/ARCHITECTURE.md, do not leave dependency vulnerability numbers as if they are permanent architecture facts.
   Either move the audit note into a clearly historical wording, or rewrite it as:
     - Historical dependency audit note, 2026-06-07: ...
     - Re-run npm audit for current results before acting on these numbers.
   Do not remove useful history completely unless another docs file already owns audit history.
   The Architecture doc should focus on stable architecture, not current vulnerability counts.

4. Add Query/Pinia migration rule
   In docs/ARCHITECTURE.md, under Server-State Caching or wherever the Query section lives, add a short "Migration rule" note:
     - TanStack Query should own server/API state and cache.
     - Pinia should own UI/navigation state.
     - Do not add new Query → Pinia duplicated server-state flows unless needed for compatibility.
     - Existing /api/scan flow is currently hybrid for compatibility and should be migrated carefully in a separate PR.
     - New server data flows should prefer TanStack Query as the source of truth.
   Keep this concise and practical.

5. Verify docs consistency
   Confirm docs consistently describe:
     - desktop/tablet grid: @tanstack/vue-virtual
     - mobile grid: native scroll
     - PhotoSwipe 5 owns image viewer/lightbox gestures
     - VSBS owns only mobile metadata sheet drag/snap/scroll
     - Fuse.js is only local lightweight filtering/helper behavior
     - backend /api/search owns active unified search results
     - TanStack Query currently caches /api/scan
     - Pinia currently still owns visible folders/images and UI/navigation state

6. Avoid unnecessary changes
   Do not reformat whole files.
   Do not rewrite entire sections unless needed.
   Keep changes minimal and reviewable.

Verification:
   grep -R "vue-virtual-scroller\|RecycleScroller\|DynamicScroller\|\.vue-recycle-scroller" README.md docs/ frontend/src/ 2>/dev/null || echo "No stale references"

   Acceptable result:
     - No stale docs references to removed library/selector.
     - If any reference remains, it must be explicitly historical and clearly marked as old behavior.

   Run frontend build only if touched files require it; docs-only changes do not require a build.

Final report:
1. Files read.
2. Files changed.
3. Exact stale references removed/updated.
4. Confirmation that Query/Pinia migration rule was added.
5. Verification grep result.
6. Whether build was run or skipped, and why.

Commit if clean:
git add README.md docs/
git commit -m "docs: clean up TanStack migration notes"
git push origin refactor/tanstack-virtual-scroll

Do not merge into main.
