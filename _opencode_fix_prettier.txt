Fix Prettier formatting in 4 E2E test files at /home/ubuntu/gallery-repo/frontend.

Run:
```
cd /home/ubuntu/gallery-repo/frontend && pnpm prettier --write \
  tests/e2e/breadcrumb.spec.ts \
  tests/e2e/index-status-panel.spec.ts \
  tests/e2e/library-management.spec.ts \
  tests/e2e/metadata-performance.spec.ts
```

Then verify with:
```
cd /home/ubuntu/gallery-repo/frontend && pnpm prettier --check \
  tests/e2e/breadcrumb.spec.ts \
  tests/e2e/index-status-panel.spec.ts \
  tests/e2e/library-management.spec.ts \
  tests/e2e/metadata-performance.spec.ts
```

Then commit and push:
```
cd /home/ubuntu/gallery-repo && git add -A && git commit -m "style: fix prettier formatting in e2e test files" && git push origin main
```
